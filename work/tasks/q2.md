# q2 实时待办

## 2026-08-28 Q1 坐标约定确认

- [x] 用户确认 Q1 以扫描速率为 X 轴；据此复核 Q2“两问对比”图，当前唯一有效配对为左面板 A/B、A/C，右面板 A/D、A/E，A 为扫描速率（K/min），B:E 为对应达阈时间（s）。
- [x] 历史说明（已废止）：此前曾将边界敏感性写成横向配对点并把扫描速率设为分类 Y；当前已统一改为扫描速率 X 的双面板趋势曲线。未改 CSV、模型、正文或 Origin 工程，未提交、未推送。
- [ ] `workflow.py finish q2 --no-fetch` 受工作区既有责任域外改动拦截；待责任域整理后再运行完整结束门禁。

## 2026-08-28 边界敏感性图构图调整

- [x] 根据用户反馈取消单面板四系列散点方案；改为双面板 `Line + Symbol` 敏感性曲线。左面板为 t₁₅，右面板为 t₁₀，扫描速率继续作 X，PMV/Kuwabara 各一条曲线，5 K/min 做重点标注。
- [x] 在两条模型曲线之间增加可编辑的模型差异带（或细竖向连接线）作为视觉层次；5 K/min 标注差异百分点。未添加统计误差棒：当前无重复实验或随机样本，有限体积/模态差异继续作为数值验证。
- [x] 只调整绘图报告中的构图和 Origin 操作说明，保留 `边界敏感性.csv` 原始 3×5 数据，不新增插值点或未经确认的方法。
- [x] 生成 Python 构图预览 `output/build/边界敏感性预览/边界敏感性预览.png` 及源脚本，并完成目视检查；预览不替代 Origin 原生图，不改 CSV、模型或正文。
- [x] 明确敏感性图坐标轴名称：X 为“扫描速率（K/min）”；左 Y 为“t₁₅相对变化（%）”；右 Y 为“t₁₀相对变化（%）”；图注说明相对 ISO 主关系。

## 2026-08-28 主结果与两问对比数据交付

- [x] 按已核对的 ISO 设置重导出 5 K/min 模态历程，464 点至115.75 s；两个首次交点与冻结阈值一致。保真门禁、数值运行门禁均 PASS，登记 Q2-ISO-HISTORY-20260828 仅冻结数值复现。
- [x] 新增 `figures/editable/降温过程.csv`（464×4）与 `两问对比.csv`（3×7），分别对应一张独立图。对比包含2/5/10 K/min下两问的t₁₅、t₁₀及相对Q1的缩短幅度；使用工作表公式计算，表格运行门禁 PASS。
- [x] 核对 Q1/Q2 材料、初温、DSC处理、潜热和阈值定义，保留题设h_b与外边界差异。基准t₁₅、t₁₀分别缩短62.2167%、62.8273%，不解释为风速单因素效应。更新绘图报告和交接，未改Q1、正文、旧CSV或Origin工程，未提交推送。
- [x] 历史记录（已废止）：曾将两问对比误写为左面板 B/A、C/A、右面板 D/A、E/A；该方案与 Q1 的“扫描速率作 X 轴”约定不一致，不得采用。F/G 仍仅作标签来源，不作为绘图系列。
- [x] 根据 Q1 图轴约定修正两问对比 X/Y：A（扫描速率）统一作 X，左面板为 A/B、A/C，右面板为 A/D、A/E；采用与 Q1 一致的 Line+Symbol，F/G 仅作标签来源。此前横向 B/A 设计废止，不再作为当前画法。
- [ ] 提交可移植代码前补齐 Q2 工作树缺失的原始 DSC 附件；本轮只读使用 Q1 工作树原件并在运行报告记录完整路径，未跨责任域复制原件。

## 2026-08-28 以主结果为中心重新设计

- [x] 核对建模意见和现有数据：Q2 有 ISO 阈值结果，但现存温度历程、剖面属于 PMV 对照，不能直接作为主模型过程图；远端 feature/q2 仍为 101c1bc。
- [x] 历史构图记录（已由后续方案替代）：主图改为“Q2 贴身侧降温过程与阈值事件＋基准工况的 Q1/Q2 阈值对照”，扫描速率三工况保留小表；边界敏感性当时曾写为横向配对点图。当前边界敏感性已改为双面板扫描速率趋势曲线，保持 ISO 主关系、Q1 命名、用户目录和一图一表，不改数据、正文或手工工程。
- [x] 制图前导出并审计 ISO、5 K/min 的模态温度历程，与已核对的 t₁₅=103.4915847342144 s、t₁₀=115.66576634720914 s 对照；未沿用 PMV 历程或从两个阈值点拟合曲线。
- [ ] 按已交付的独立降温过程、两问对比表制作 Origin 原生图并验证联动；同步正文图注与引用，保留主模型登记待审核状态。数据和说明已完成，图页尚未制作。
- 本轮未渲染、未操作 Origin、未提交或推送；原有4项模型/论文审核继续保留。

## 2026-08-28 与 Q1 统一图表命名

- [x] 核对 Q1 的敏感性、温度曲线 CSV 和对应用户成图，将 Q2 的共用表头统一为“扫描速率（K/min）”“t₁₅（s）”“t₁₀（s）”；间隔直接写“t₁₀−t₁₅（s）”，百分比指标保留必要含义。
- [x] 同步三份 CSV、既有生成入口、绘图说明和结果交接；Q1/Q2 三个共用列名逐字一致，Q2 三份 CSV 除首行外逐字不变。
- 未重画图片或操作 Origin，不改 Q1、模型、主次关系和 ISO 比较基准，不提交推送；原有4项待审核保持不变。

## 2026-08-28 按建模意见调整 Origin 表格

- [x] 阈值时间表改为 ISO 主关系专用的四列宽表；敏感性表改为 PMV/Kuwabara 相对 ISO 主关系的变化；数值验证表缩为 ISO 的方法复核与加密检查六列。三份文件仍在 `figures/editable/`，不另放副本。
- [x] 从冻结核算及验证源逐项核对：阈值不变，敏感性按 ISO 分母计算，验证保留带符号方法偏差与加密绝对变化；更新唯一绘图报告及结果交接。本次仅排表，不渲染、不重算模型、不改正文和手工 Origin 工程，不提交或推送。
- 后续：正文敏感性图注仍需从 PMV 分母同步为 ISO；正式图和旧设计预览尚未按新列布局重制，不能直接套用。主模型登记仍待审核，原有4项待审核不关闭。

## 2026-08-28 按建模手意见复核主图定位

- [x] 只读核对远端：`feature/q2=101c1bc`，`feature/paper-shell=a6c1eee`；重新阅读 `work/reviews/q2_20260827.md`、`work/reviews/q2_drawing_20260827.md`、`results/论文整改交接_20260827.md` 与当前正文。
- 依据优先级：用户已明确建模手意见最重要，论文手只是转述。模型选择、主次关系、比较基准以建模手的明确意见为准；正文、绘图报告或已有 CSV 与之冲突时应记录并同步纠正，不能倒过来用旧稿覆盖建模结论。
- 结论：建立一个三层防护服瞬态传热模型，主外边界取 ISO 11079 关系对应的 `h_e=18.46`；PMV/Kuwabara 仅用于外对流相关式敏感性对照，不是三个并列主模型。上轮沿用三关系核算表逐图美化，没有按建模意见重新筛选主结果与对照内容；那三张预览不得直接视为已通过主次内容审核的正式论文图。
- 后续建议：主结果图只展示 ISO 关系下的阈值时间，突出 DSC 基准 5 K/min；2/10 K/min 是同一模型下的 DSC 参数情景，不是另外两个模型。PMV/Kuwabara 比较集中于灵敏度分析或附录；数值验证优先呈现主模型的复核与加密结果，其他工况可作为补充资料。图数服从信息需要，不固定为三张。
- 需要复核：绘图审核第8--12行明确要求以 ISO 主关系为敏感性基准；当前正文图注和 CSV 仍以 PMV 为分母。后续按明确审核意见统一图、表、正文分母，不仅改图例。此次只核对并记录，未修改模型、数据、正文或图形，未提交/推送；原有4项待审核保留。

## 2026-08-28 远端同步与模型状态核查
- [x] 应用户要求快进拉取 `origin/feature/q2` 至 `101c1bc`，本次远端仅新增 `results/论文整改交接_20260827.md`，不改变正文、模型或数值；本地未提交 CSV、图、报告和预览文件均保留。
- 已弃用：动态人体热容节点及人体温度 ODE；持续/无限 `q_pcm(T)` 功率源；以人体温度 `T_h` 而非贴身侧 `T_in=T_1(0,t)` 判断15/10℃阈值。`9b5250f` 已在交接中禁止，`26bf2e6` 已替换正文；当前 Q1 远端任务也明确禁止持续功率源。
- 主模型与对照：`91d96f4` 已禁止把 PMV 的 `h_e=20.96`、`t15=92.67s` 继续作为最终主结果；`44a9649` 审核第7、17行明确主外边界采用 `18.46`，PMV 与 Kuwabara **仅为敏感性对照**，不得并列成三个主模型。没有查到要求彻底取消这两个对照关系的记录。
- `101c1bc` 明确不改模型、不重算；第26行仍保留 PMV/Kuwabara 对照，第47行要求去掉“旧PMV”的研发历史命名。后续制图必须先区分主结果与对照，不仅是更换图例文字。
- 需要复核：当前 ISO 主模型登记仍为 `DRAFT + NEEDS_REVIEW`，不能把模型方向通过或对照计算冻结说成整问终稿冻结；下方旧任务与交接中的“正文尚未同步”等历史句子不能取代当前正文与审核结论。已有敏感性基准矛盾另列于 PDF 预览记录，本次仅核查，不改模型、图表或正文，原有4项待审核未关闭。

## 2026-08-28 当前稿 PDF 预览
- [x] 应用户要求，从当前唯一正文 `paper/q2.tex` 直接编译独立预览，正文9页、当前参考文献1页，输出 `output/build/q2-preview/第二问当前稿.pdf`。原正文、数据、SVG/OPJU 均未改动；仅在临时入口将未定义的 `FigureFullWidth` 兼容到现有 `FigureDoubleWidth`，并转换正文实际引用的 SVG 用于排版。两遍编译后无未解析引用，10页已检查；第一页原有一处行宽溢出警告和浮动图页留白保留，非终稿验收。
- 需要复核：第8页敏感性 SVG 仍以 ISO 为基准，图注与当前 CSV 已改成 PMV 基准，两者不一致；用户目录中的新 PNG/OPJU 尚未被正文引用。本次保留现状供用户检查，未擅自换图。
- 需要复核：远端 `101c1bc` 新增论文整改交接，要求“旧PMV”改成“PMV对流关系/PMV对照关系”；当前正文尚未落实。图例应明确相关式名称，ISO 的简化假设在正文或图注说明，不以单个“简化”“旧”作为正式命名。
- 说明：当前 `q2.tex` 没有引用已有的 `q2_algorithm.tex`，故本次原样预览不含伪代码；原有4项待审核事项保持不变。

## 2026-08-28 图形重设计

- [x] 直接读取现有三份 `figures/editable/` CSV，在 `output/build/图形重设计/` 制作阈值区间图、敏感性热图和数值验证矩阵设计预览，提供逐图 PNG/SVG 及 `绘图设计.py`。ISO 主关系突出显示，PMV/Kuwabara 仅作对照；敏感性继续以 PMV 为比较分母。
- [x] 更新唯一绘图报告 `modules/30_q2/figures/绘图报告.md`，标明设计稿路径、输入表、复现命令和实现范围；未改论文、求解模型、CSV、原有正式图或用户手工 OPJU/PNG。
- [x] 三张 PNG 已目视检查；SVG 矢量检查和阈值/敏感性/方法偏差逐项数值检查通过。`git diff --check`、冲突标记检查通过；`workflow.py finish q2` 在临时设置 `core.quotepath=false` 后通过，原先中文路径转义造成的误报已排除，未写入 Git 配置。
- 阻塞：已有 Origin 实例的活动工程接口返回 `0x800401E3`，无法安全附着。按工程保护规则停止操作；未新开实例、重启、覆盖或保存用户工程。当前只有 Python 设计预览，Origin 原生数据绑定与联动验收未完成。
- 需要复核：采用新版图时须同步正文引用；原敏感性 SVG 的 ISO 分母与正文/当前 CSV 的 PMV 分母矛盾尚未通过换图消除。原有4项模型/论文待审核保持不变，本轮不提交、不推送。

## 本轮绘图
- [x] 以当前冻结的外对流核算与有限体积验证为输入，用 Python 交付阈值时间、边界敏感性和数值验证三图、逐图数据、绘图报告与运行审计。
- [x] 此前 Python 数据与绘图运行均为 `PASS`；当时预览与文字边界检查已通过。原审计记录保留，本轮不重新渲染；当前绘图交接入口为 `modules/30_q2/figures/绘图报告.md`。
- [x] 按用户指定的 Q1 目录结构，将三份 Origin 宽表统一移动为 `figures/editable/阈值时间.csv`、`边界敏感性.csv`、`数值验证.csv`，移动前后内容一致；报告移到 `figures/绘图报告.md` 并整体改为简洁的当前 CSV 说明，更新所有引用，不留旧位置副本。模型、原有 Python 输入 CSV 和图像未改动；不渲染、不启动 Origin。
- [x] 按用户最新要求与当前论文统一三份 Origin CSV 列名：模型简称＋β_DSC / t₁₅ / t₁₀＋单位，区分相对变化、方法相对偏差和加密相对变化绝对值。本次去掉问题编号上标，三份表的数据行均不变。
- [x] 按用户要求与当前论文图注一致，将 Origin 敏感性比较基准从 ISO 改为旧 PMV：B/C 改为 ISO 简化两阈值，D/E 仍为 Kuwabara，直接取冻结“较旧值变化（%）”列并独立复算。此要求覆盖此前绘图意见的 ISO 零基准，不更改求解模型或论文主模型；报告与交接已同步。
- 本轮仅制作相关式对照和数值验证图，不改求解模型及正文。`44a9649` 审核已建议主模型使用 `18.46`，但正文与最终结果登记尚未同步；图不作为 ISO 附录公式或物理适用性的验证。

## 当前继承关系
- Q2 继承 Q1 冻结模型：人体为 `37 ℃` 恒温边界，阈值变量为贴身侧 `T_in=T_1(0,t)`；中间层采用有限、不可逆相变有效比热。
- 轻微运动的人体侧修正继续使用附件直接给出的 `h_b=4.0 W/(m²·K)`；不额外引入未给定的代谢热参数。
- `v=2/5/10 K/min` 的 DSC 潜热口径继续与 Q1 一致。

## 已有结果（暂保留，不冻结）
- 旧 Q2 外侧强制对流采用 `h_for=12.1 sqrt(v)`，在 `v=3 m/s` 下 `h_for=20.9578 W/(m²·K)`。
- 该口径下 `v_DSC=2/5/10 K/min` 的 `t15=171.403/92.670/66.481 s`，数值收敛和有限体积验证均已通过。
- **从 2026-08-27 起，这组三个时间只作为“旧外对流关系对照结果”，不得继续登记为最终 Q2 结论，直到外侧对流模型重新核定。**

## P0：外侧风速换热关系重新溯源并重建
### 1. 已完成的来源追踪
- `h_c=max{2.38|T_cl-T_a|^0.25, 12.1 sqrt(v_ar)}` 是 Fanger/PMV 热舒适模型体系中的自然--强制对流关系，后被 ISO 7730、ASHRAE 55 等热舒适标准采用；它并不是针对南极 `-40 ℃` 防护服实验专门建立的相关式。
- `12.1 sqrt(v)` 的实验谱系可追溯到 Winslow、Gagge、Herrington 1939 年关于穿衣人体空气流动换热的实验工作（DOI `10.1152/ajplegacy.1939.127.3.505`）。后续 PMV 模型将其作为强制对流支路。
- 附件 2 本身给出的无风关系 `2.38(T_cl-T_a)^0.25` 恰好也是上述 PMV 体系的自然对流支路，因此旧 Q2 采用其配套的 `12.1 sqrt(v)` 有来源上的连续性，但**适用场景仍偏向热舒适/一般环境，不能因此直接证明适用于 `-40 ℃`。**

### 2. 更合适的冷环境候选模型
- 优先考察 `ISO 11079:2007`（冷环境热应激/IREQ），该标准明确用于冷环境、室内外工作，推荐参数范围包含 `T_a<=10 ℃`、`0.4<=v_a<=18 m/s`，因此 `-40 ℃, 3 m/s` 落在其温度方向和风速范围内。
- ISO 11079 的边界空气层修正为
  `I_a,r = 0.092 exp(-0.15 v_a - 0.22 v_w) - 0.0045  [m²K/W]`
  （式 A.13），并通过
  `h_c = f_cl/I_a,r - h_r`
  （式 A.9）得到对流系数。
- 本题明确忽略辐射，同时附件 2 说明本文不考虑服装面积因子/有效对流面积修正，因此在与题设保持一致的简化下，可先取 `h_r=0, f_cl=1`。若只把环境风 `v_a=3 m/s` 纳入外侧边界、避免与附件的“轻微运动 h_b=4”重复计数，则先取 `v_w=0`，得到
  `I_a,r≈0.05416 m²K/W`，`h_c≈18.46 W/(m²·K)`。
- 该 `18.46` 不是最终值，而是**当前首选重算候选**；需要编程手用它重跑 Q2，并与旧 `20.96` 对照。
- 不要擅自用 ISO 11079 的 `v_w(M)` 运动修正式，除非建模端决定引入代谢率 `M`；否则会与附件已经通过 `h_b=4` 表征的轻微运动发生重复计数。

### 3. 独立经验对照
- Kuwabara 等 2005 热人模室外风实验给出适用于约 `0.1--4.7 m/s` 的全身经验关系，文献汇总常写为 `h_c=3.4+6.9 v^0.9`；在 `v=3 m/s` 时约 `21.95 W/(m²·K)`。
- Holmér、Nilsson、Havenith、Parsons 1999 关于衣服在风与人体运动下换热的研究指出，静态衣服热阻会因风和运动下降，并提出供标准使用的动态修正；该方向可作为“为何 Q2 不能只把静态无风系数机械替换成一个室内舒适公式”的理论依据。
- 以上经验关系只用于交叉核对，不直接替代 ISO 11079 冷环境主候选，除非后续文献适用范围审查发现更强证据。

## 编程手立即执行
- [x] 补齐三种外对流关系、三组 DSC 扫描速率共 9 个工况的独立有限体积验证，并对 ISO 11079 的 5 K/min 工况检查时间步与网格收敛。
- [x] **暂停把 `12.1 sqrt(3)=20.96` 作为唯一正式 Q2 外边界。** 旧登记已降为 `DRAFT + NEEDS_REVIEW`，旧结果仅作对照。
- [x] 新增 `ISO11079` 外边界工况，按 `h_e=18.46 W/(m²·K)` 重跑 `v_DSC=2/5/10 K/min` 的 `t15/t10`。
- [x] 新增 `h_e=21.95 W/(m²·K)` 的 Kuwabara 2005 全身室外风经验对照。
- [x] 已输出 `results/外对流核算.csv`，字段包含外对流模型、`h_e`、DSC 扫描速率、`t15/t10` 及相对旧值变化。
- [x] 三组结果差异明显：ISO 11079 候选相对旧 PMV 结果延长约 `11.36%--12.09%`，Kuwabara 对照缩短约 `3.77%--4.00%`，故外侧换热相关式列为 Q2 的主要不确定来源。
- [x] 已检查固定 `h_e`：三组计算全过程的自然支路最大值约为 `7.049 W/(m²·K)`，低于最小强制候选 `18.46 W/(m²·K)`，没有时段发生自然支路反超。
- [x] 根据最终选定关系更新 `modules/30_q2/paper/q2.tex`、结果登记和图表；旧 `92.67 s` 已降级为对照值。

## 外对流核算结果

| 外对流模型 | `h_e`/(W/(m²·K)) | DSC扫描速率/(K/min) | `t15`/s | `t10`/s | `t15`较旧值变化 |
|---|---:|---:|---:|---:|---:|
| ISO 11079 简化 | 18.46 | 2 | 192.028 | 204.262 | 12.033% |
| ISO 11079 简化 | 18.46 | 5 | 103.492 | 115.666 | 11.678% |
| ISO 11079 简化 | 18.46 | 10 | 74.035 | 86.167 | 11.362% |
| 旧 PMV 关系 | 20.958 | 2 | 171.403 | 182.234 | 0 |
| 旧 PMV 关系 | 20.958 | 5 | 92.670 | 103.439 | 0 |
| 旧 PMV 关系 | 20.958 | 10 | 66.481 | 77.207 | 0 |
| Kuwabara 经验关系 | 21.95 | 2 | 164.575 | 174.946 | -3.984% |
| Kuwabara 经验关系 | 21.95 | 5 | 89.084 | 99.393 | -3.869% |
| Kuwabara 经验关系 | 21.95 | 10 | 63.977 | 74.242 | -3.766% |

## NEEDS_REVIEW
- [ ] 需要核对 ISO 11079 简化 `f_cl=1,h_r=0` 与附件"忽略服装面积因子、题面忽略辐射"的语义是否完全一致；若附件这里的"服装面积因子"另有所指，再调整。
- [ ] 轻微运动只通过 `h_b=4` 表征是否足够，暂按题面最小参数原则保留，不另加代谢热。
- [ ] 需要复核：正文 `q2.tex` 中的模型汇总公式格式是否完全符合 `model_summary.md` 标准（零文字规则、单大括号公式）。
- [ ] 需要复核：`q2_algorithm.tex` 伪代码中的控制结构关键字（for/if/while）是否与 `q1_algorithm.tex` 保持一致，以及数学动作描述是否充分。
- [x] 需要复核：`q2.tex` 中新增的 `\cite{iso11079}` 和 `\cite{kuwabara2005}` 是否已加入 `references.tex` 的 bib 条目中。
- [x] 已添加 `iso11079`、`iso7730`、`kuwabara2005`、`yang2023hc`、`xu2022clothingwind` 到 `references.tex`。
- [x] 当前 `results/registry.csv` 尚未登记 ISO 11079 主模型结果（`t15=192.028/103.492/74.035 s`），需新增 registry 条目。
- [x] 已新增 `Q2-ISO11079-MAIN-20260827` registry 条目。
- [x] 三张图（阈值时间、边界敏感性、数值验证）的正文引用和解释是否到位。
- [x] 已在结果和灵敏度分析中引用三张图，并添加图注。`

## DONE
- [x] Q2 已同步 Q1 的恒温人体边界、贴身侧阈值和有限相变模型。
- [x] 旧 `12.1 sqrt(v)` 工况的模态/有限体积收敛验证已完成，现作为外对流相关式对照基线保留。
- [x] 在 `q2.tex` 的"问题二完整传热模型"小节末尾添加 `\subsubsection{模型汇总}`，使用无目标函数单大括号公式格式汇总三层 PDE 系统、Robin 边界、界面连续条件和初始条件。
- [x] 编写 `q2_algorithm.tex`：实现 Q2 微小时间段拓展分离变量法的 algorithm2e 伪代码，包含输入参数、预求本征值、主时间循环（冻结有效比热、求解 2×2 界面斜率方程、模态展开推进、阈值检测）和输出。
- [x] 更新 `work/tasks/q2.md` 同步完成项和复核项。
- [x] 单独完成 `M=60`、`dt=0.25 s`、Gauss 积分点 `160` 的三组扫描速率核算，代码与结果分别为 `code/核算M60.py` 和 `results/M60核算.csv`。
- [x] 完成 `18.46/20.96/21.95 W/(m²·K)` 三组外对流关系的 9 工况核算，代码与结果分别为 `code/外对流核算.py` 和 `results/外对流核算.csv`。
- [x] 完成上述 9 工况的独立有限体积复核，阈值最大偏差为 `1.2146%`；ISO 11079、5 K/min 工况的有限体积加密检查最大变化为 `0.0036%`。代码与结果分别为 `code/外对流有限体积验证.py` 和 `results/外对流有限体积验证.csv`。
- [x] 重写 `q2.tex`：主外边界改为 ISO 11079 简化 `h_e=18.46`，模型汇总界面坐标修复为 `x1=d1, x2=d1+d2`，主结果表改为 ISO 候选值，`v` 拆分为 `v_a`(空气风速)和 `β_DSC`(DSC扫描速率)，算法小节增加插值说明。
- [x] 重写 `q2_algorithm.tex`：`h_e` 改为 `18.46`，`v` 改为 `β_DSC`，增加阈值线性插值函数 `InterpThresh`。
- [x] 升级 skill 至 v1.3.0，按 output guard 重写 q2.tex：X.1 标题改为"问题的描述与分析"，增加 `X.6 灵敏度分析` 独立章节，增加三张图的引用和解释，补全 `references.tex` 缺少的 bib 条目，新增 `registry.csv` 的 ISO 11079 主模型登记项。
