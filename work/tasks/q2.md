# q2 实时待办

> 本文件只保留当前有效模型口径和未完成事项。

## 当前有效模型口径
- 主模型名称：**有风轻微运动条件下的三层非稳态导热模型**。
- Q2 继承 Q1 的三层导热、`37 ℃` 恒温人体边界、贴身侧评价温度 `T_in=T_1(0,t)` 与有限不可逆相变模型。
- 环境空气速度 `v_a=3 m/s`；轻微运动人体侧换热系数取附件给定的 `h_b=4.0 W/(m²·K)`。
- 外侧基准边界采用 ISO 11079 冷环境边界空气层热阻关系，在题设忽略辐射、等有效面积简化下换算为 `h_e=18.46 W/(m²·K)`。
- PMV 与 Kuwabara 关系仅用于外对流相关式敏感性对照。
- DSC 扫描速率：`5 K/min` 为基准，`2/10 K/min` 为敏感性工况。

## 当前结果
- ISO 11079 简化关系下 `β_DSC=2/5/10 K/min`：
  - `t15=192.028/103.492/74.035 s`
  - `t10=204.262/115.666/86.167 s`
- 基准返回室内时间为 `103.492 s`。
- 三关系×三扫描速率独立有限体积复核最大模态/FVM阈值偏差 `1.2146%`；ISO、5 K/min 加密检查最大变化 `0.0036%`。

## 当前待办
- [x] [HUMAN.MODEL_NAMING_SENTENCE] 用户已明确采用“有风轻微运动条件下的三层非稳态导热模型”。
- [ ] [HUMAN.Q2_MAIN_BOUNDARY] 人工复核 ISO 11079 简化中的 `h_r=0`、`f_cl=1` 与题面语义一致性；确认后再完成最终冻结。
- [ ] [RESULT.REGISTRY_SYNC] 正文已使用 `Q2-ISO11079-MAIN-20260827` 结果，人工确认主边界后同步 registry 的正式状态与 `used_in_paper`。
- [ ] 恢复/统一可复现所需的 DSC 原始附件接口，正式复算不得依赖某台机器外部绝对路径。
- [ ] 全文合并后执行语言/结构 guard 与页面级预览。

## Agent 执行约束
- 不恢复 PMV 主模型、动态人体节点或持续 PCM 功率源。
- Q2 正文只维护 `\cite{...}` 使用位置；参考文献条目由 `feature/references` 维护。
- 若交接文件与本 task 冲突，以当前正文、当前结果和用户指令为准。
- `HUMAN.*` 仅在用户明确确认后更新。