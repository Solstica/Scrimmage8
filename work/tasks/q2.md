# q2 实时待办

## 当前建模决策
- Q2 完整继承 Q1 的人体单节点等效热容 + 三层一维瞬态导热 + 中间功能层温度相关放热模型，不重新建立材料内部控制方程。
- Q2 只修改两个对流边界：人体状态由静止站立改为轻微运动；外部环境由无风改为 `v=3 m/s`。
- 人体--内层织物对流按附件表格取 `h_b=4.0 W/(m²·K)`；人体 ODE 改为 `C_h dT_h/dt=-A_b*4.0*[T_h-T_1(0,t)]`。
- 轻微运动当前只通过附件明确给出的 `h_b: 3.0 -> 4.0` 进入模型，不额外加入题目未给出的代谢产热参数。若后续题面/国标全文明确要求独立代谢热，再单独复核。
- 服装外表面对流采用自然/强制两支统一关系：`h_e=max{2.38|T_cl-T_inf|^0.25, 12.1*sqrt(v)}`。其中 `2.38|Delta T|^0.25` 是 Q1 无风自然对流支路，`12.1*sqrt(v)` 是有限风速下强制对流支路。
- Q2 取 `v=3 m/s`，得到 `h_for=20.96 W/(m²·K)`；初始最大温差 `77 K` 时自然对流支路仅约 `7.05 W/(m²·K)`，随后随衣表降温进一步减小，因此主要计算区间内强制对流始终占优，可取 `h_e=20.96 W/(m²·K)`。
- 温差并未从有风模型中消失：外侧热流仍为 `q''=h_e[T_3(L,t)-T_inf]`；只是强制对流占优后 `h_e` 本身主要由风速决定。
- Q2 继续采用 Q1 的微小时间段拓展分离变量法。第二层 Dirichlet--Dirichlet + PCM 热源处理不变；第一层 Robin 系数改为 `4.0`；第三层 Robin 系数固定为 `20.96`，因此第三层本征值不再逐时间步更新。
- 返回室内时间仍取首次 `T_h<=15 ℃` 的 `t15^(2)`；同时记录 `t10^(2)`。

## 文献使用边界
- `ISO 7730:2005`：用于服装表面对流系数 `max{2.38|T_cl-T_a|^0.25,12.1 sqrt(v)}` 的自然/强制对流两支关系。
- Yang & Zhang, 2023, International Journal of Thermal Sciences：0.2--20 m/s 热人模 CFD 显示全身 `h_c` 随空气速度按幂函数增加，3 m/s 位于研究风速范围内；用于验证风速效应和 `h_c` 数量级，不直接替代本题服装表面经验式。
- Xu et al., 2022, International Journal of Heat and Mass Transfer：穿衣人体 CFD 表明环境风会通过服装开口及衣下空气层增强热交换；用于说明 Q2 有风后必须修正 Q1 静止空气边界，不直接提供本题外表面 `h_e(3)` 数值。

## TODO
- [ ] 在 Q1 求解器稳定后复用同一代码内核，增加 Q2 参数组 `h_b=4.0, v=3.0, h_e=20.96`。
- [ ] 输出 `T_h(t)`、三层温度场、`t15^(2)`、`t10^(2)`，并与 Q1 对比。
- [ ] 做 `Delta t`、模态截断数 `M` 收敛检查。
- [ ] 以完整 `max{h_nat,h_for}` 关系与固定 `20.96` 简化各计算一次，确认两者阈值时间一致或差异可忽略。
- [ ] 完成 Q1/Q2 人体温度曲线和外侧热流对比图。

## NEEDS_REVIEW
- [ ] `12.1 sqrt(v)` 关系源自服装表面热舒适/人体热平衡体系，南极低温条件超出典型室内热舒适标定环境；正文保留 Yang 2023 与 Xu 2022 作为风速效应和穿衣机制的交叉依据，并在最终稳健性检验中比较替代风速关系。
- [ ] 若后续需要严格区分强制与混合对流，可用 `Ri=Gr/Re^2` 或其他混合对流判据做补充验证；当前 `v=3 m/s` 下强制支路远大于自然支路，因此不增加该复杂度。

## DONE
- [x] 完成 Q2 问题分析和闭合传热模型。
- [x] 明确 Q1 自然对流公式与 Q2 强制对流支路的关系。
- [x] 明确轻微运动只采用附件给出的 `h_b=4.0` 修正。
- [x] 将 Q2 求解结构压缩为 Q1 求解器的参数更新，而非另建一套算法。
