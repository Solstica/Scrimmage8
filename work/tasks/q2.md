# q2 实时待办

> 本文件只保留当前有效模型口径和未完成事项。历史绘图尝试、旧 PMV 主模型记录、已废止结构规则均已移出实时 task，避免继续影响后续 Agent。

## 当前有效模型口径
- Q2 继承 Q1 的三层瞬态导热、`37 ℃` 恒温人体边界、贴身侧评价温度 `T_in=T_1(0,t)` 与有限不可逆相变模型。
- 环境空气速度记为 `v_a=3 m/s`；轻微运动人体侧换热系数取附件给定的 `h_b=4.0 W/(m²·K)`。
- 主外侧边界采用 ISO 11079 冷环境边界层关系的题设简化，当前 `h_e=18.46 W/(m²·K)`。
- PMV 对流关系与 Kuwabara 经验关系仅用于外对流相关式敏感性对照，不得写成并列主模型，也不使用“旧 PMV”之类研发历史命名。
- DSC 扫描速率统一记为 `β_DSC`；基准 `5 K/min`，`2/10 K/min` 为敏感性工况。

## 当前主结果
- ISO 主关系下 `β_DSC=2/5/10 K/min`：
  - `t15=192.028/103.492/74.035 s`
  - `t10=204.262/115.666/86.167 s`
- 基准 `β_DSC=5 K/min` 的返回室内时间为 `103.492 s`。
- 三关系×三扫描速率的独立有限体积复核最大模态/FVM阈值偏差 `1.2146%`；ISO、5 K/min 加密检查最大变化 `0.0036%`。

## v1.4 正文状态
- [x] `2.2` 已整理为“预备工作”，集中外表面对流系数与人体侧换热参数。
- [x] `2.3` 已整理为唯一主模型“有风轻微运动条件下三层防护服瞬态传热模型”。
- [x] 模型汇总改为 X.3 内最后一个编号大分点，汇总标题后零文字；两处界面坐标分别为 `x_1=d_1`、`x_2=d_1+d_2`。
- [x] GPT 留档式否定句与“旧 PMV”研发历史语言已清理。
- [x] `q2_algorithm.tex` 已统一为 `Input / Initialize / Output / Return` 与 `for/while/if/else` 英文关键字，数学动作中文；删除模态算法中的“单元”措辞。
- [x] `2.5` 第一段第一句直接报告基准 `t15=103.492 s`；外对流比较放入灵敏度分析，FVM复算独立为数值检验。

## 当前待办
- [ ] [HUMAN.MODEL_NAMING_SENTENCE] 人工复核“综上，我们建立有风轻微运动条件下三层防护服瞬态传热模型。”是否与最终 PDE、双侧边界和题面输出一致；**仅人工可勾选。**
- [ ] [HUMAN.Q2_MAIN_BOUNDARY] 人工复核 ISO 11079 简化中的 `h_r=0`、`f_cl=1` 与题面“忽略辐射/面积口径”的语义一致性；确认后再把整问主模型从 `NEEDS_REVIEW` 转为最终冻结。
- [ ] 恢复/统一可复现所需的 DSC 原始附件接口；当前不得依赖某台机器的外部绝对路径完成正式复算。
- [ ] 最终 Origin 图接入正文时，敏感性图必须以 ISO 主关系为零基准，PMV/Kuwabara 只作为对照；图注与 CSV 分母保持一致。
- [ ] 全文合并后执行一次语言/结构 guard 与页面级预览。

## Agent 执行约束
- 不从历史 `[x]` 记录恢复已废止的 PMV 主模型、动态人体节点或持续 PCM 功率源。
- 若交接文件与本 task 冲突，以最新 `results/论文整改交接_20260827.md`、当前正文和用户指令为准。
- `HUMAN.*` 项不得由 Agent 自动勾选。
