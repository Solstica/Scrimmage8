# q2 实时待办

## 当前建模决策
- Q2 继承 Q1 冻结模型 `Q1-3a1982f-20260826`：人体侧为固定 `T_b=37 degC` 的 Robin 边界，阈值变量为贴身侧温度 `T_in=T_1(0,t)`，不再使用动态人体热容节点。
- 三层材料仍采用一维瞬态导热，层间温度和热流连续；中间功能层采用端点线性 DSC 基线和有限、不可逆固化过程，不把 DSC 曲线当作无限功率源。
- Q2 只修改两个对流参数：轻微运动取 `h_b=4.0 W/(m2 K)`；风速 `v=3 m/s` 时外侧采用 `h_e=max{2.38|T_cl-T_inf|^0.25,12.1 sqrt(v)}`。
- 初始最大温差 `77 K` 时自然对流支路约为 `7.05 W/(m2 K)`，小于强制对流支路 `12.1 sqrt(3)=20.9578 W/(m2 K)`；随衣表温度下降自然支路进一步减小，因此正式求解固定采用强制支路。独立验证已确认完整 `max` 关系给出相同阈值时间。
- DSC 修正面积为 `20385.458 W K/kg`。扫描速率取 `2, 5, 10 K/min`，对应潜热分别为 `611563.74, 244625.50, 122312.75 J/kg`。
- 正式方法为微小时间段拓展分离变量法：`dt=0.25 s`、每层 `M=40` 个模态、Gauss 积分点 `160`。第一层和第三层采用 Robin 本征函数，第二层采用 Dirichlet--Dirichlet 正弦基；每步通过 `2x2` 线性方程保持两处界面热流连续。

## 正式结果

模型 ID：`WHUT13-Q2-THERMAL`

模型版本：`d50852b+Q1-3a1982f`

结果 ID：`Q2-d50852b-Q1-3a1982f-20260827`

| DSC 扫描速率（K/min） | 潜热（kJ/kg） | `t15`（s） | `t10`（s） |
|---:|---:|---:|---:|
| 2 | 611.564 | 171.403 | 182.234 |
| 5 | 244.625 | 92.670 | 103.439 |
| 10 | 122.313 | 66.481 | 77.207 |

正式运行由外部 `run_gate.py` 启动，状态为 `PASS`。正式结果表、温度历程、温度剖面、运行报告和验证表均以简短中文文件名保存在 `modules/30_q2/results/`。

## 验证结论
- 模态时间步减半至 `dt=0.125 s` 后，5 K/min 情形的 `t15`、`t10` 相对正式解分别变化 `-0.0127%`、`-0.0154%`。
- 模态数增加至 `M=60` 后，5 K/min 情形的 `t15`、`t10` 相对正式解分别变化 `0.2843%`、`0.2776%`。
- 独立隐式有限体积法在三种扫描速率下与正式模态解的阈值时间偏差不超过 `1.20%`；有限体积法自身减半步长、加密网格后的变化均小于 `0.005%`。
- 有限体积 Newton 方程最大残差小于 `1.9e-10`，固化进度未倒退，累计潜热不超过材料潜热上界。
- 完整 `max{h_nat,h_for}` 与固定 `h_for` 的 5 K/min 阈值时间完全一致。

## TODO
- [ ] 根据 Q1/Q2 的正式结果生成两问贴身侧温度曲线和外侧热流对比图，并为每张正文图建立独立数据表。

## NEEDS_REVIEW
- [ ] `12.1 sqrt(v)` 关系源自服装表面热舒适/人体热平衡体系，南极低温条件超出典型室内标定环境。正式结论只能表述为在题设采用的经验关系下所得结果，不得宣称完成了实地实验验证。
- [ ] DSC 扫描速率仍是缺失实验条件；Q2 正文必须同时保留 `2--10 K/min` 敏感性区间，不能把 `92.67 s` 写成无条件唯一答案。

## DONE
- [x] 完成 Q1 冻结语义向 Q2 的模型接口同步和两轮保真门禁，均为 `PASS`。
- [x] 完成 Q2 正式求解脚本 `modules/30_q2/code/q2.py`。
- [x] 完成独立验证脚本 `modules/30_q2/code/check_q2.py`。
- [x] 完成三种 DSC 扫描速率的 `t15`、`t10`、温度历程和温度剖面计算。
- [x] 完成时间步、模态数、有限体积步长/网格及完整外对流关系验证。
- [x] 完成外部运行门禁、结果归档和论文交接说明。
- [x] 将 `modules/30_q2/paper/q2.tex` 同步为冻结 Q1 语义、有限相变模型、正式 Q2 算法与结果，删除旧动态人体节点和持续 PCM 功率源表述。
- [x] 单独完成 `M=60`、`dt=0.25 s`、Gauss 积分点 `160` 的三组扫描速率核算，代码与结果分别为 `code/核算M60.py` 和 `results/M60核算.csv`；相对正式 `M=40` 结果的最大阈值偏差为 `0.308%`。
