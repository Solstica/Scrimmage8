# q4 实时待办

## 当前建模结构
- 2026-08-27 接口更新：继承 Q3 提交 `764375a` 的 8 扫描速率×双阈值诊断。按 `(v,阈值)` 关联，不自行确定最终阈值；旧三速率15℃结果保留为对照。
- Q4 承接 Q3，以 Q3 最优实际坚持时间 `t3*(v)` 为性能基准；不追加材料资金，因此采用原始厚度 `d1=0.7 mm,d2=0.4 mm,d3=0.3 mm`，只提高中间功能层单位质量放热能力。
- 设倍率 `alpha>=1`，提高比例 `r=alpha-1`。
- "各温度下同比例提高放热能力"继续解释为 `p_alpha(T)=alpha p(T)`；归一化相变权重 `w(T)` 与固化进度形状不变，总潜热变为 `L_pcm^(alpha)=alpha L_pcm`。
- 第二层有效比热：`c_eff^(alpha)(T)=c2+alpha*L_pcm*w(T)`；禁止回到无限持续功率源。
- 给定 `alpha,v` 时调用 Q1 正演；实际时间还需与 Q3 相同承重口径比较。
- 优化仍为单变量：在每个 `(DSC扫描速率v,阈值)` 场景下，反求最小 `alpha*`，使 Q4 实际坚持时间不小于同一速率、同一阈值下的 Q3 最优时间。

## 当前双阈值诊断结果（DRAFT）
- [x] 已继承 Q3 `764375a`，完成8速率×15℃/10℃共16场景真实反求。新文件 `modules/50_q4/results/双阈值结果.csv` 不覆盖旧三速率结果。
- 5场景无需强化：`v=0.5/1`的两种阈值，以及`v=1.25`的10℃；均 `alpha=1`。
- `v=5`下15℃/10℃提高比例分别为 `86.328125%/108.984375%`；`v=10`分别为 `157.287598%/203.375244%`。
- [x] 16场景共38个原始倍率/二分边界工况独立复算通过，结果在 `双阈值验证.csv`，外部记录 `审核/双阈值验证.json` 为 PASS；最大倍率区间宽度 `6.103515625e-5`。
- 主求解进程退出0，但包装器中文输出解码失败；已如实记录 `审核/运行说明.md`，不伪造主运行门禁PASS。
- 仍待最终阈值和可信参数范围确认；现有M=60对照只覆盖旧v=5、15℃场景，不能当作本轮全部16场景的网格复核。

## 已有第一版结果（仅基准对照，不冻结）
- 仅在 `v=5 K/min`、Q3 使用 15℃阈值的阶段口径下，已有 `t3*=416.037901 s`，`alpha*=1.86328125`，提高比例 `86.328125%`。
- `M=60` 对照得到 `85.552979%`，与 `M=40` 相差约 `0.775` 个百分点；该差异作为数值敏感性保留，不是当前首要问题。
- **从 2026-08-27 起，`86.33%` 明确降级为 `v=5 K/min` 单一条件下的阶段结果，不能作为 Q4 唯一最终答案。**

## 旧三速率阶段结果（DRAFT，保留对照）
- 已按同一扫描速率逐行匹配 Q1 潜热与 Q3 当前 15℃阶段目标：`v=2/5/10 K/min` 的最小提高比例分别为 `43.786621%/86.328125%/157.287598%`。
- 对应最小倍率为 `1.4378662109375/1.86328125/2.5728759765625`，Q4 实际时间为 `701.204036/416.042764/321.061069 s`。
- 三个速率下原始厚度负重上限均为 `784.915666 s`，均高于对应 Q3 目标，因此全部可行且由热安全约束控制。
- 三组二分区间宽度均为 `6.103515625e-5`；逐组验证表明下界不可行、上界可行，且抽样热安全时间随倍率单调增加。
- 结果文件：`modules/50_q4/results/q4_result.csv`；验证文件：`modules/50_q4/results/q4_validation.csv`。Q3 最终目标阈值与低速可信域未锁定前不冻结。

## P0：全部 DSC 扫描速率逐一反求
- [x] 已补齐现有主扫描速率 `v=2,5,10 K/min` 三组 Q4 反求；每组均匹配同一 `v` 下 Q1 的 `L_pcm(v)` 和 Q3 的 `t3*(v)`。
- [x] Q4 代码已改为读取 Q3 最优结果表，不再写死 `v=5` 与单一目标时间；结果表新增 Q3 最优层数、二分上下界、状态和活动约束。
- [ ] Q3 低速扩展与最优层数重新审查完成后，用最终结果表再次运行全部可信扫描速率。
- [ ] 如果 Q3 最终主目标从 `15 ℃` 调整为 `10 ℃`，或者同时报告 `t_eff15/t_eff10` 两套口径，Q4 必须严格继承同一目标定义；不能用 Q3 的 10℃最优时间去匹配 Q4 的 15℃时间，反之亦然。
- [x] 每个速率均先检查固定负重上限；代码已实现 `INFEASIBLE_WEIGHT` 显式输出分支，当前三组均通过可行性检查。
- [x] 每个可行速率已独立执行"上界扩张 + 二分"，并输出所要求的跨速率字段及二分上下界。
- [x] 已在 `alpha=1,1.5,alpha*,2` 及二分下界处逐速率检查单调性与最小性，未发现平台或异常反向变化。

## 与 Q3 的耦合要求
- Q4 不再把 Q3 的 `n=3` 当作预先固定事实；它只读取 Q3 在每个扫描速率下的最终 `n*(v)` 和 `t3*(v)`。
- 如果 Q3 在低速侧出现 `n=3->2->1...` 的结构切换，Q4 目标时间也要随之更新。Q4 的目的始终是"原始厚度 + 提升 PCM 能力"去追平**同一参数条件下 Q3 的最佳方案**。
- "不追加资金"继续解释为不采用 Q3 的额外增厚预算、恢复原始厚度，仅提升材料本征放热能力；否则若保留 Q3 最优增厚，`alpha=1` 已经达到 `t3*`，第四问会退化成 0%，不符合题目设置。

## 数值设置
- [x] 主结果继续与 Q1--Q3 统一使用 `dt=0.25 s,M=40,nq=160`；`M=60` 仅保留为已有数值敏感性证据。
- [x] 二分倍率容差为 `1e-4`，三组百分比区间宽度均小于 `0.01` 个百分点，并已保存上下边界工况。

## 正文与冻结
- 2026-08-27 推送同步：保留远端 `80378a7` 的正文和伪代码更新；本轮双阈值结果及图表仍为 DRAFT，尚未替换正文中的阶段结果，不能据正文完成状态视为数值冻结。
- [x] `q4.tex` 已按照 `model_summary.md` 标准重构，使用 `\subsubsection{模型汇总}` 零文字公式模板。
- [x] `q4_algorithm.tex` 已创建，包含二分搜索算法伪代码。
- [x] 正文已填入跨扫描速率结果表（表\ref{tab:q4-results}）和单调性验证表（表\ref{tab:q4-validation}）。
- [ ] 等 Q3 的目标阈值、扫描速率可信域和最优层数重新锁定后，再更新 `q4.tex` 中的数值结果。
- [ ] 正文不得只写"提高 86.33%"；当前已给出跨扫描速率结果/区间，并解释倍率对缺失 DSC 条件是否敏感。
- [ ] 当前 `q4.tex` 末尾仍含"Q1 潜热尚待核定、Q3 待重新求解"的过期文字，最终必须删除。

## 图表演示
- [x] 已生成双面板 Python 演示图：左图比较 `v=2/5/10 K/min` 下的最小提高比例，右图绘制相应最优倍率下的有效比热曲线，并标出主相变区间与峰温。
- 图的论证作用：扫描速率改变潜热换算及追平 Q3 所需的最小倍率；倍率只放大潜热贡献，不改变 Q1 给出的相变温区与峰温位置。
- 文件：`modules/50_q4/figures/第四问演示图.png`、`modules/50_q4/figures/第四问演示图.svg`；逐图数据：`modules/50_q4/tables/第四问演示图数据.csv`；脚本：`modules/50_q4/code/第四问绘图.py`。
- 当前图只使用 `Q4-1.1-20260827` 的 `DRAFT + NEEDS_REVIEW` 阶段结果，不能直接作为冻结正文图；Q3 最终目标与可信扫描速率域锁定后必须重跑。

## 图组扩展（本轮）
- [x] 已核对四张新增候选图的论证任务与真实数据可用性，方案见 `modules/50_q4/results/绘图方案.md`。
- [x] 生成潜热需求、约束裕量、相变响应和达标边界图，保持逐图最小数据表。
- [x] 完成数据公式、热安全/实际时间区分、二分边界、相变积分及视觉验收；四图均可从独立CSV逐像素重绘。
- 图组只展示已有模型与实际结果；不补造速率连续面、不作统计置信声明、不改变正式求解路线。

## Q3 新接口（本轮）
- [x] 已将 `q4.py` 输入改为 `粗扫描最优方案.csv`，从配套 `粗扫描方案.csv` 按 `(v,n*)` 补取潜热，并核对相同阈值有效时间。
- [x] 阈值逐行传入正演器；支持16个场景、可变层数及 `alpha=1`。旧无阈值表必须显式指定阈值，禁止默认为15℃。
- [x] 新运行结果使用 `双阈值结果.csv`；验证输出增加阈值及实际时间，避免跨阈值串行和把热安全时间误当实际时间。
- [x] 完成16场景接口映射与分支回归、全部真实求解和38工况边界复核；新结果仍不冻结。
- 新版验证仅复算每个场景的原始倍率与最终二分边界；`alpha=1`时只需复核已达到变量下界的可行性，不额外生成无助于最小性判断的高倍率样本。
- [x] 新增 `双阈值需求.png/.svg/.csv`，展示全部16场景及对应Q3层数；此前四图只作为旧三速率15℃对照。五张新增图均通过数据、独立重绘及目视检查。

## DONE
- [x] 已建立有限相变框架下的同比例放热倍率模型。
- [x] 已完成 `v=5 K/min` 第一版真实运行、二分边界验证、时间步和模态截断检查。
- [x] 已完成 `v=2/5/10 K/min` 表驱动逐组反求，正式运行门禁状态 `PASS`。
- [x] 已完成三组单调性、二分最小性和数值残差验证，验证运行门禁状态 `PASS`。
- [x] `q4.tex` 已完成完整撰写：描述与分析、DSC增强、传热正演、优化模型、模型汇总（\subsubsection模板）、二分搜索求解、结果与分析。
- [x] `q4_algorithm.tex` 已创建，包含二分搜索伪代码。
- [x] `q3.tex` 已完成完整撰写：描述与分析、可行域与约束、优化模型、模型汇总（\subsubsection模板）、黄金分割搜索求解、结果与分析框架。
- [x] `q3_algorithm.tex` 已创建，包含黄金分割搜索伪代码。
