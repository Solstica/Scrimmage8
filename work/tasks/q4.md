# q4 实时待办

## 当前建模决策
- Q4 承接 Q3，以 Q3 最优实际坚持时间 `t3*` 为性能基准；本问不追加材料资金，因此不通过增加厚度改善性能，而是固定原始三层厚度 `d1=0.7 mm, d2=0.4 mm, d3=0.3 mm`，只提高中间功能层材料的单位质量放热能力。
- 设放热能力倍率 `alpha>=1`，提高比例为 `r=alpha-1`，最终报告 `100*r%`。
- Q1 已将 DSC 由持续功率源修正为有限相变过程。对基线校正后的正放热峰 `p(T)` 定义归一化权重 `w(T)=p(T)/∫p(T)dT`，相对固化进度 `xi_DSC(T)=∫_T^Ts w(theta)dtheta`。
- “各温度下同比例增加放热能力”解释为 `p_alpha(T)=alpha*p(T)`。由于归一化时倍率相消，`w(T)` 和 `xi_DSC(T)` 的温度分配形状不变；总潜热尺度变为 `L_pcm^(alpha)=alpha*L_pcm`。
- 因此第二层有效比热改为 `c_eff^(alpha)(T)=c2+alpha*L_pcm*w(T)`。这既体现所有温度下同比例增强，也保留有限总能量和“固化完成后不再放热”。禁止重新回到 `rho*q(T)` 的无限持续功率源写法。
- Q4 工况继承 Q1：`T_inf=-40 ℃`、无风、静止站立，人体作为 `37 ℃` 恒温热源，内侧 `h_b=3.0 W/(m²·K)`，阈值评价量为贴身侧温度 `T_in=T_1(0,t)`。
- 给定 `alpha` 时，调用 Q1 正演得到 `t_T^(4)(alpha)=inf{t:T_in<=15 ℃}`。
- Q4 不改变服装质量，因此负重约束对应的时间上限 `t_W^(4)` 与 `alpha` 无关；承重公式本身沿用 Q3 最终确认口径，不在 Q4 重复另定。
- 实际坚持时间 `t_eff^(4)(alpha)=min(t_T^(4)(alpha), t_W^(4))`。
- 优化模型：`min alpha-1`, s.t. `t_eff^(4)(alpha)>=t3*`, `alpha>=1`。
- 先检查可行性：若 `t_W^(4)<t3*`，则单纯提高放热能力无论多大都无法达到 Q3 基准；若 `t_W^(4)>=t3*`，只需反求最小 `alpha` 使 `t_T^(4)(alpha)>=t3*`。
- 在固定结构与边界下，增大 `alpha` 增加相变区间有效热容和总潜热，因此 `t_T^(4)(alpha)`应为非减函数。采用“扩大上界 + 二分搜索”反求最小倍率，无需遗传算法等启发式方法。
- 最终结果 `r_min=100*(alpha*-1)%`；同时输出 `t3*`、`alpha*`、`t_T^(4)(alpha*)`、`t_eff^(4)(alpha*)`。

## 与 Q1/Q3 的依赖
- Q1 已条件冻结有限相变正演；Q4 第一版采用其基线 `v=5 K/min` 与 `L_pcm=244625.496 J/kg`。该倍率结果仍条件于 DSC 扫描速率假设。
- Q3 已验证三种扫描速率方案；Q4 与 Q1 基线一致，采用 `v=5 K/min` 行的 `t3*=416.0379012473915 s`。Q3 当前结果状态为 `VALIDATED + CHECKED`，尚未随正文转为 `FROZEN`。
- 原始厚度服装质量 `m_c=0.754216692 kg`，由 Q3 承重公式得到固定上限 `t_W^(4)=784.91566616 s`，大于 `t3*`，因此 Q4 可行且最优点由热安全约束控制。

## 代码草稿与当前缺口
- `modules/50_q4/code/q4.py` 已接入 Q1 正演器，按 `alpha*L_pcm` 更新有限相变有效比热，并完成承重可行性、无预设倍率上限的上界扩张和二分反求。
- 第一版主运行结果：`alpha*=1.86328125`，最小提高比例 `86.328125%`；`t_T=416.04276393956764 s`，`t_eff=416.04276393956764 s`，热安全约束起控制作用。
- 二分下侧 `alpha=1.86318125` 时 `t_T=416.02614920653554 s<t3*`，上侧正式解满足约束，边界定位正确。
- 时间步减半到 `dt=0.125 s` 时倍率不变；增加到 `M=60` 时倍率为 `1.85552978515625`，提高比例为 `85.552978515625%`，与第一版相差 `0.775146484375` 个百分点。
- 结果文件为 `modules/50_q4/results/q4_result.csv`，验证文件为 `modules/50_q4/results/q4_validation.csv`；当前登记为 `DRAFT + NEEDS_REVIEW`，尚未写入正文。

## TODO
- [x] 接入 Q1 的有限相变正演并固定 `v=5 K/min` 基线。
- [x] 读取 Q3 的 `v=5 K/min` 最优时间 `t3*`。
- [x] 计算原始厚度下固定的 `t_W^(4)` 并完成可行性检查。
- [x] 抽取若干倍率验证 `t_T^(4)(alpha)` 单调性和二分边界。
- [x] 减小时间步并提高模态数，检查 `alpha*` 的数值敏感性。
- [ ] 复核 `M=40` 与 `M=60` 的 `0.775` 个百分点差异，决定终稿沿用 Q1 正式设置还是提高模态数后重跑 Q1--Q4。
- [ ] 待 Q3 正文同步并冻结后，将 Q4 结果转为 `VALIDATED/FROZEN` 并写入 `q4.tex`。
- [ ] 结果冻结后再按论文需要决定是否绘制倍率--坚持时间曲线；不为凑图提前生成。

## NEEDS_REVIEW
- [ ] “不追加资金”当前按“不再增加材料用量、采用原始厚度，只提升材料本征放热能力”解释；这与题意将 Q4 作为新材料研究课题的表述一致。若赛题说明另有费用定义，再同步约束。
- [ ] `p_alpha(T)=alpha p(T)` 在有限相变口径下等价于 `L_pcm` 同比例提高、相对固化进度形状不变；论文中需避免把 `w(T)` 也再次乘 `alpha`造成重复计数。
- [ ] 若最终 Q3 的 `t3*` 恰由承重约束而非热安全约束控制，Q4 的目标仍按实际 `t3*` 比较，但需在结果分析中指出提高放热能力只能改善热学约束，不能突破固定负重上限。
- [x] Q1 正演接口时间单位固定为秒；搜索阶段只把“目标时刻前未越阈值”解释为可行，不再写成 `inf`，选定倍率后继续推进到真实越阈值时刻。
- [ ] 正式设置 `M=40` 与加密设置 `M=60` 分别给出 `86.328125%` 和 `85.552979%`；需统一 Q1--Q4 的最终模态截断后再冻结数字。

## DONE
- [x] 建立 Q4 单变量放热倍率优化模型。
- [x] 将“各温度同比例增强”与 Q1 的 DSC 有限相变模型一致地映射为总潜热同比例放大。
- [x] 选定上界扩张 + 二分搜索作为最小倍率求解算法。
- [x] 明确 Q4 对 Q1 `L_pcm` 和 Q3 最优时间 `t3*` 的依赖关系。
- [x] 完成 Q4 极简代码草稿，包含原始倍率检查、承重不可行分支、无预设倍率上限的上界扩张和二分搜索。
- [x] 用临时单调函数完成机械冒烟测试：原始倍率满足、二分反求和承重不可行三个分支均按预期执行；未生成模型结果。
- [x] 通过数学建模编程手保真门禁，未发现目标、约束、边界或搜索域漂移。
- [x] 完成 Q4 第一版真实运行：最小提高比例 `86.328125%`，运行审计状态 `PASS`。
- [x] 完成单调性、活动约束、时间步和模态截断验证；验证运行审计状态 `PASS`。
