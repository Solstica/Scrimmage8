# q4 实时待办

> 本文件只保留当前有效模型口径和未完成事项。

## 当前有效模型口径
- 主模型名称：**以最小放热强化率为目标的相变材料热传导参数反问题模型**。
- Q4 继承 Q3 室外工况，并按相同 `(β_DSC, θ)` 匹配问题三最优有效持续时间 `t^*_{3,θ}(β_DSC)`。
- 为比较结构增厚与材料性能强化两种设计路径，本问恢复原始三层厚度 `0.7/0.4/0.3 mm` 作为结构基准，并保持材料支出不变；只提高中间功能层单位质量放热能力。
- 放热倍率 `α≥1`，`p_α(T)=αp(T)`，总潜热 `αL_pcm`，有效比热 `c_eff^(α)=c_2+αL_pcm w(T)`；相变历史约束保持有限、不可逆。
- 实际持续时间 `t^(4)_{eff,θ}(α)=min{t^(4)_{T,θ}(α),t_W^(4)}`，目标为最小化 `α-1`，并满足 `t^(4)_{eff,θ}(α;β_DSC)≥t^*_{3,θ}(β_DSC)`。
- 搜索区间先做倍率预采样确认单调不减，再采用可行性检查、上界扩张与二分搜索。
- DSC 扫描速率层级统一为：`5 K/min` 基准，`2/10 K/min` 敏感性，`0.5–1.75 K/min` 扩展诊断。

## 当前结果
- 基准 `β_DSC=5 K/min`：15℃ `α*=1.863281`、提高 `86.328%`；10℃ `α*=2.089844`、提高 `108.984%`。
- 敏感性：`β_DSC=2 K/min` 对应 `43.787%/52.594%`；`β_DSC=10 K/min` 对应 `157.288%/203.375%`。
- 原始厚度负重上限 `t_W^(4)=784.916 s`。
- `0.5–1.75 K/min` 仅作为低速扩展诊断；其中低速场景存在 `α*=1`，用于观察活动约束切换。
- 二分边界验证已完成；非零强化场景上下界满足“下界未达标、上界达标”。

## 当前正文图层级
- 主体保留流程图与相变材料本构图。
- 基准及敏感性正式答案由紧凑结果表直接报告。
- “强化需求热图”移入低速扩展诊断；“约束轨迹图”不再进入当前正文。

## 当前待办
- [x] [HUMAN.MODEL_NAMING_SENTENCE] 用户已明确采用“以最小放热强化率为目标的相变材料热传导参数反问题模型”。
- [ ] [HUMAN.Q3_THRESHOLD_INTERFACE] Q3 最终阈值锁定后，确认 Q4 终稿保留双阈值还是裁剪为同一主阈值；始终按同一 `(β_DSC,θ)` 匹配目标时间。
- [ ] [RESULT.REGISTRY_SYNC] Q3 阈值接口确认后，将真正进入终稿的 Q4 基准/敏感性结果同步为正式 registry 状态与真实 `used_in_paper`。
- [ ] 当前16个计算场景已完成二分边界复核，但仅局部场景完成 `M=60` 或半时间步检查；终稿不得扩写为“全部场景均完成网格/步长收敛”。
- [ ] 若 Q3 最终结果或可信扫描速率域变化，重新由 Q3 正式结果接口驱动 Q4 反求并更新终稿表/图。
- [ ] 全文合并后执行语言/结构 guard 与页面级预览。

## Agent 执行约束
- 不恢复旧单阈值阶段、研发留档式否定句或全英文 Algorithm 规则。
- Algorithm 固定程序关键字英文，算法名称、输入输出内容与数学动作中文；不使用未定义的 `\SetKwWhile`。
- DSC 扫描速率统一记为 `β_DSC`，不得解释为现场相变持续时间。
- Q3 正文/Algorithm/task 只通过正式结果接口继承，不在 Q4 分支直接修改。
- X.1 正文已按“任务 → 困难 → 思路”重写：删除“为比较结构增厚与材料性能强化两种设计路径”留档式对比句，改为正向“保持原始三层厚度与材料支出不变，仅通过提高中间功能层单位质量放热能力达到目标坚持时间”；模型命名句已从“4. 模型汇总”区移出，汇总区零自然语言（audit：HARD=0）；协作者 `8353655` 曾将命名句放回汇总标题后，已再次修复。
- 正文仅保留 3 张图且已分散：X.1 流程图（[H]）、X.3 相变联合图（材料本构）、X.6 强化需求热图（扩展诊断），彼此间隔公式/算法/表格；每图描述独立成段（material 图描述对应热图+切片+峰温三面板，dual 图描述独立为图后分析段），无“一段分析多图”。
- 若交接文件与本 task 冲突，以当前正文、当前结果和用户指令为准。
- `HUMAN.*` 仅在用户明确确认后更新。