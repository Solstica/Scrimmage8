# q4 实时待办

> 本文件只保留当前有效口径和未完成事项。历史 Q4-1.1 单阈值阶段记录、过期同步状态和已废止写作规则已从实时 task 移除。

## 当前有效模型口径
- Q4 继承 Q3 的室外工况，并按相同 `(β_DSC, θ)` 匹配问题三最优有效持续时间 `t^*_{3,θ}(β_DSC)`。
- 材料支出固定为原始服装方案，三层厚度固定 `0.7/0.4/0.3 mm`；只提高中间功能层单位质量放热能力。
- 放热倍率 `α≥1`，`p_α(T)=αp(T)`；归一化相变权重和固化进度形状不变，总潜热变为 `αL_pcm`。
- 有效比热为 `c_eff^(α)=c_2+αL_pcm w(T)`，继续采用有限、不可逆相变历史约束。
- 对 `θ∈{15℃,10℃}` 定义 `t^(4)_{T,θ}(α)`，实际持续时间 `t^(4)_{eff,θ}(α)=min{t^(4)_{T,θ}(α),t_W^(4)}`。
- 优化目标为最小化 `α-1`，约束 `t^(4)_{eff,θ}(α;β_DSC)≥t^*_{3,θ}(β_DSC)`。

## 当前结果
- 基准 `β_DSC=5 K/min`：
  - 15℃口径：`α*=1.863281`，最小提高比例 `86.328%`；
  - 10℃口径：`α*=2.089844`，最小提高比例 `108.984%`。
- 8 个扫描速率 × 2 个阈值共16场景均已完成真实反求；低速场景存在 `α*=1` 的零强化分支。
- 原始厚度负重上限 `t_W^(4)=784.916 s`。
- 二分边界验证已完成；非零强化场景上下界满足“下界未达标、上界达标”。

## v1.4 正文状态
- [x] 已建立 `4.2 预备工作`：DSC同比例增强、给定 `α` 的传热正演、坚持时间函数。
- [x] `4.3` 只保留决策变量、目标函数、约束条件和模型汇总。
- [x] 开头“但不再追加资金”等留档式否定改为当前方案的正向表述。
- [x] `r=α-1`、二分中点和每段 `c_eff/α_2` 更新已行内化/合并，减少短公式碎片。
- [x] 热安全时间已参数化为 `t^(4)_{T,θ}`，与16场景双阈值结果一致。
- [x] `q4_algorithm.tex` 已采用“固定关键字英文 + 数学动作中文”，并补齐负重可行性、`α=1`零强化和有效时间二分逻辑。
- [x] 结果分析已修正两处逻辑：10℃需求由Q3同阈值目标时间决定；活动约束在 `β_DSC=1.25 K/min` 时按阈值分别判定。

## 当前待办
- [ ] [HUMAN.MODEL_NAMING_SENTENCE] 人工复核“综上，我们建立最小放热能力提高比例模型。”是否与 `α`、最小提高比例目标和同场景坚持时间约束一致；**仅人工可勾选。**
- [ ] [HUMAN.Q3_THRESHOLD_INTERFACE] Q3 最终阈值口径锁定后，确认 Q4 终稿保留双口径还是裁剪为同一主阈值；不得跨阈值匹配目标时间。
- [ ] 若 Q3 最终正式结果或可信扫描速率域发生变化，重新从 Q3 最终结果表驱动 Q4 反求并更新终稿表/图；不得手工挑选旧阶段结果。
- [ ] 正文最终选图优先保留“约束裕量”，其次考虑“相变响应”；“达标边界”更适合作为验证/附录，“潜热需求”若与结果表重复可不占正文主图位置。
- [ ] 当前16场景已完成二分边界复核，但并未对全部场景逐一完成 `M=60` 或半时间步收敛检查；终稿不得把局部模态敏感性外推为“全部16场景均完成网格/步长收敛”。
- [ ] `feature/q4` 只维护 Q4 自身正文、算法、结果和 task；Q3 正文/Algorithm/task 只通过 Q3 正式结果接口继承，不在 Q4 分支直接修改。
- [ ] 全文合并后重新执行语言/结构 guard 与页面级预览。

## Agent 执行约束
- 不恢复“正文顶部内部状态”“Q1潜热尚待核定”“Q3待重算”等过期留档文字。
- DSC扫描速率统一记为 `β_DSC`；不得把实验扫描速率解释为现场相变持续时间。
- 若交接文件与本 task 冲突，以最新 `results/论文整改交接_20260827.md`、当前正文和用户指令为准。
- 审核结论直接去重归并本 task；不得在 `work/reviews/` 新建 Q4 审核状态文件。
- `HUMAN.*` 项不得由 Agent 自动勾选。
