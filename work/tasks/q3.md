# q3 实时待办

## 当前建模决策
- 问题三完全继承冻结 Q1 的无风、静止、$-40\,^{\circ}\mathrm C$三层瞬态传热模型：人体为 $37\,^{\circ}\mathrm C$ 恒温边界，以贴身侧 $T_{in}=T_1(0,t)$ 首次降至 $15\,^{\circ}\mathrm C$ 为热安全阈值，中间层采用有限潜热与不可逆固化口径；Q3 只改变最外隔热层厚度。
- 按附件原文“后期增加厚度只调整最外层厚度（最外层涂层每层厚度规定固定 0.3mm）”，不把三层厚度同时连续优化。决策变量为新增外层涂层数 `n>=0` 的整数，`d3(n)=0.3(1+n) mm`；`d1=0.7 mm,d2=0.4 mm` 固定，中间层 `0.4<0.45 mm` 的硬度约束自动满足。
- 建模手已确认继续采用 Q1 的面积口径 `A=A_b=1.6521 m²`。原服装材料成本按附件价格计算：内层 500 元/500g、外层 150 元/500g、中间层 10 元/m²，得到 `C0≈301.67 元`。
- 每新增一层 0.3mm 外涂层增加质量 `Delta_m≈0.148689 kg`、增加费用 `Delta_C≈44.61 元`。50% 增资约束给出 `n<=floor(0.5*C0/Delta_C)=3`，因此实际候选只有 `n={0,1,2,3}`，对应外层厚度 0.3/0.6/0.9/1.2mm。使用完全枚举，不引入 GA/SQP 等不必要优化算法。
- 方案 `n` 下服装质量 `m_c(n)=A[rho1*d1+rho2*d2+rho3*d3(n)]`，基准约 `0.7542 kg`，每新增一层增加 `0.148689 kg`。
- 建模手已确认“最大承受重量100kg”包含人体自重，即人体自身 60kg + 防护服质量需满足 `60+m_c(n)<=100-0.05*t`；对应负重上限时间 `t_W(n)=20[40-m_c(n)] s`。
- 对每个 n 调用 Q1 正演模型，仅更新最外层厚度和总域长度，得到贴身侧温度首次降至 15℃ 的热安全时间 `t_T(n)`。
- 建模手已确认 `v=2/5/10 K/min` 三种扫描速率全部计算并分别选优。有效持续时间定义为 `t_eff(n)=min(t_T(n),t_W(n))`，每种速率分别取 `n*=argmax t_eff(n)`。

## TODO
- [ ] 将 Q3 正文同步为冻结 Q1 的恒温人体边界、`T_in` 阈值和有限相变模型，并写入已验证结果。
- [ ] 形成厚度--热安全时间与厚度--负重时间的对比图，若两类时间在候选区间内交叉则标出。

## NEEDS_REVIEW
- [ ] 当前 Q3 正文仍沿用动态人体热容节点、`T_h` 阈值和旧 `q_pcm(T)` 持续功率源，尚不能把本轮结果登记为论文可用的 `FROZEN`。

## DONE
- [x] 将问题三转化为“Q1 正演 + 离散厚度枚举 + 时间衰减负重约束”的有限优化模型。
- [x] 利用附件价格和固定 0.3mm 涂层规则，将预算约束提前化简为四个候选方案。
- [x] 完成 q3 正文模型建立，未提前填写热安全时间和最优厚度结果。
- [x] 完成 Q3 极简枚举脚本草稿：已实现四个候选的厚度、总厚度、服装质量、材料费用和负重时间计算，并为 Q1 热安全正演保留唯一 `get_tT(d3)` 接口。
- [x] 核对已闭合计算：候选外层厚度为 `0.3/0.6/0.9/1.2 mm`，费用约 `301.67/346.28/390.89/435.49 元`，当前主口径下负重时间约 `784.92/781.94/778.97/775.99 s`。
- [x] 建模手确认承重包含人体自重、用料面积取 `A=A_b`，并要求 `2/5/10 K/min` 三种扫描速率全部计算。
- [x] 接入冻结 Q1 热正演内核，完成四个厚度乘三种扫描速率的 12 个工况枚举；正式表格和文件名均使用中文。
- [x] 三种扫描速率均选出 `n=3,d_3=1.2 mm`；有效时间分别为 `701.195/416.038/321.060 s`，均由热安全约束限制。
- [x] 基准厚度三组结果与冻结 Q1 完全一致；最优厚度时间步减半最大差异 `0.003409%`，模态数增加最大差异 `0.294009%`。
