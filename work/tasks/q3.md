# q3 实时待办

## 当前建模结构
- Q3 在 Q1 传热模型基础上，允许增加中间功能层厚度以延长坚持时间，受材料费用和人体负重双重约束。
- 决策变量为功能层厚度 `d2`，外层 `d1=0.7 mm` 和内层 `d3=0.3 mm` 不变。
- 费用约束：`C(d2)=rho1*c1*d1+rho2*c2*d2+rho3*c3*d3 <= Cmax`。
- 负重约束：`tW(d2)=20.0*(40.0 - A*m(d2))`，总质量随 `d2` 递增，负重时间随 `d2` 递减。
- 实际坚持时间：`teff(d2)=min{tT(d2), tW(d2)}`，单峰函数，采用黄金分割搜索求解。
- 模型汇总按 `model_summary.md` 标准使用 `\subsubsection{模型汇总}` 零文字公式模板。

## 待办
- [ ] 完善材料参数（c1,c2,c3,rho1,rho2,rho3,A）的数值来源和核定。
- [ ] 完成费用上限 `Cmax` 和承重上限 `m_max` 的确定。
- [ ] 完成 Q3 代码实现和数值求解，填入结果表。
- [ ] 低速扫描速率下的最优层数扩展审查。
- [ ] 若最终主目标从 `15℃` 调整为 `10℃`，重新求解并更新。
- [ ] 关键结果进入正文前完成 FROZEN+CHECKED 规定检查。

## DONE
- [x] `q3.tex` 已完成完整撰写：描述与分析、可行域与约束、优化模型、模型汇总（\subsubsection模板）、黄金分割搜索求解、结果与分析框架。
- [x] `q3_algorithm.tex` 已创建，包含黄金分割搜索伪代码。