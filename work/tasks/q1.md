# q1 实时待办

## 当前建模决策
- 已锁定方案 A：三层一维瞬态导热 + 微小时间段拓展分离变量法。
- 三层沿厚度方向等面积平板化，完整未知量保留为温度场 `T_i(x,t)`。
- 人体按附件 2 作为恒温热源 `T_b=37 ℃`；不建立人体单节点热容 `T_h(t)`。
- 人体--内层织物对流取静止站立 `h_b=3.0 W/(m²·K)`，内侧边界为 `-k_1 T_x(0,t)=h_b[T_b-T_1(0,t)]`。
- 以贴身侧 `T_in(t)=T_1(0,t)` 作为人体表面附近接触界面的等效评价温度；`t15/t10` 分别取其首次降至 15℃/10℃ 的时刻。
- 人体有效表面积 `A_b=1.6521 m²`；Q1 取有效传热面积 `A=A_b`。
- 外侧无风自然对流使用附件公式 `h_e=2.38[T_3(L,t)-T_inf]^0.25`，`T_inf=-40 ℃`。
- 第一层 Robin--Dirichlet、本征展开；第二层 Dirichlet--Dirichlet；第三层 Dirichlet--Robin。每个微小时间段只有两个未知界面斜率 `beta_12,beta_23`，由两处热流连续条件闭合。

## PCM / DSC 已锁定口径
- 禁止使用旧版持续热源 `Q_2(T)=rho_2*q_pcm(T)`。
- 对基线校正后的 DSC 放热峰 `p(T)>=0`，由归一化累计峰面积构造 `xi_DSC(T)`；数值实现加入不可逆历史约束。
- 有限相变采用 `Q_lat=rho_2*L_pcm*d(xi)/dt`；单调冷却主阶段等价为 `c_eff(T)=c_2+L_pcm*p(T)/∫p(T)dT`。
- 附件 1 共 357 点，温度 `14.050--26.018 ℃`，平均点距约 `0.03362 K/point`；点距不能直接解释为扫描速率。
- 当前端点线性操作基线给出 `A_DSC=20385.458 W·K/kg`，故 `L_pcm=1223127.48/v J/kg`（`v` 单位 K/min）。
- 基准扫描速率 `v0=5 K/min`，对应 `L_pcm=244.63 kJ/kg`；同时保留 `v=2,10 K/min` 敏感性。该扫描速率是假设，不是附件事实。

## 正式结果状态
- 结果 ID：`Q1-3a1982f-20260826`，状态 `FROZEN + CHECKED`。
- `v=2/5/10 K/min` 时：`t15=520.903/273.908/191.642 s`，`t10=558.207/311.158/228.864 s`。
- 模态法与独立有限体积法阈值偏差约 `0.81%--0.99%`；时间步、网格、模态截断均已检查。
- `M=60` 三组独立核算相对正式 `M=40` 的阈值变化为 `0.26%--0.32%`，不替换冻结正式结果。

## 新增：服装初始温度灵敏度（2026-08-27）
- [ ] **保持人体边界始终为 `37 ℃` 不变，只改变三层衣料的统一初始温度 `T_0`。** 这是灵敏度分析，不改变主模型，也不重定义人体状态。
- [ ] 建议选取至少 4 个“功能层初始仍为液态”的温度，例如 `T_0=27,30,33,37 ℃`；若程序实现更方便，可在 `[27,37] ℃` 内等间距取点。`27 ℃` 取在附件 DSC 最高实测温度 `26.018 ℃` 之上，避免一开始就人为置入部分固化状态。
- [ ] 每个 `T_0` 重新计算基准 `v=5 K/min` 下 `T_in(t)` 与 `t15/t10`；必要时再对 `v=2,10` 检查是否改变趋势，但不要求把扫描速率与初温做全组合网格。
- [ ] 输出一张正文候选图：横轴时间、纵轴 `T_in`，叠加不同 `T_0` 的多条曲线，并标出 `15 ℃` 阈值；另给一个小表记录 `T_0 | t15 | t10 | 相对37℃变化`。
- [ ] 结果解释只回答“衣料预热初温对短时坚持时间的敏感程度”，不把 `T_0=37 ℃` 改写成附件事实。若灵敏度小，则保留现有主结果；若明显，则在模型局限性中说明实验前服装热状态是重要输入。

## TODO
- [ ] 完成上述衣料初始温度灵敏度图与表，并把结果写入 Q1 灵敏度分析部分。
- [ ] Q1 主结果与主模型不因本项灵敏度分析重新解冻，除非发现初始温度会改变核心结论或造成明显数量级变化。
- [ ] 后续论文中继续明确：`T_in=T_1(0,t)` 是等效评价界面；`v0=5 K/min` 是显式建模假设；绝对潜热与 DSC 归一化形状来源分开表述。

## DONE
- [x] 完成有限相变语义修正、DSC 数据诊断、模态正演、有限体积独立验证和三组扫描速率敏感性。
- [x] 完成 `M=60` 独立核算；正式结果继续统一采用 `M=40`。
