# q1 实时待办

## 当前建模决策
- 已锁定方案 A：三层一维瞬态导热 + 微小时间段拓展分离变量法。
- 三层沿厚度方向等面积平板化，完整未知量保留为温度场 `T_i(x,t)`。
- 人体按附件 2 作为恒温热源 `T_b=37 ℃`；不建立人体单节点热容 `T_h(t)`。
- 人体--内层织物对流取静止站立 `h_b=3.0 W/(m²·K)`，内侧边界为 `-k_1 T_x(0,t)=h_b[T_b-T_1(0,t)]`。
- 以贴身侧 `T_in(t)=T_1(0,t)` 作为人体表面附近接触界面的等效评价温度；`t15/t10` 分别取其首次降至 15℃/10℃ 的时刻。
- 人体有效表面积 `A_b=1.6521 m²`；Q1 取有效传热面积 `A=A_b`。
- 外侧无风自然对流使用附件公式 `h_e=2.38[T_3(L,t)-T_inf]^0.25`，`T_inf=-40 ℃`。
- 第一层 Robin--Dirichlet、本征展开；第二层 Dirichlet--Dirichlet；第三层 Dirichlet--Robin。每个微小时间段只有两个未知界面斜率 `beta_12,beta_23`，由两处热流连续条件闭合。

## PCM / DSC 已锁定口径
- 禁止使用旧版持续热源 `Q_2(T)=rho_2*q_pcm(T)`。
- 对基线校正后的 DSC 放热峰 `p(T)>=0`，由归一化累计峰面积构造 `xi_DSC(T)`；数值实现加入不可逆历史约束。
- 有限相变采用 `Q_lat=rho_2*L_pcm*d(xi)/dt`；单调冷却主阶段等价为 `c_eff(T)=c_2+L_pcm*p(T)/∫p(T)dT`。
- 附件 1 共 357 点，温度 `14.050--26.018 ℃`，平均点距约 `0.03362 K/point`；点距不能直接解释为扫描速率。
- 当前端点线性操作基线给出 `A_DSC=20385.458 W·K/kg`，故 `L_pcm=1223127.48/v J/kg`（`v` 单位 K/min）。
- 基准扫描速率 `v0=5 K/min`，对应 `L_pcm=244.63 kJ/kg`；同时保留 `v=2,10 K/min` 敏感性。该扫描速率是假设，不是附件事实。

## 正式结果状态
- 结果 ID：`Q1-3a1982f-20260826`，状态 `FROZEN + CHECKED`。
- `v=2/5/10 K/min` 时：`t15=520.903/273.908/191.642 s`，`t10=558.207/311.158/228.864 s`。
- 模态法与独立有限体积法阈值偏差约 `0.81%--0.99%`；时间步、网格、模态截断均已检查。
- `M=60` 三组独立核算相对正式 `M=40` 的阈值变化为 `0.26%--0.32%`，不替换冻结正式结果。

## 新增：服装初始温度灵敏度（2026-08-27）
- [x] **保持人体边界始终为 `37 ℃` 不变，只改变三层衣料的统一初始温度 `T_0`。** 这是灵敏度分析，不改变主模型，也不重定义人体状态。
- [x] 取 `T_0=27,30,33,37 ℃`；`27 ℃` 高于附件 DSC 最高实测温度 `26.018 ℃`，四组功能层初始均按液态处理。
- [x] 在基准 `v=5 K/min` 下重新计算各组 `T_in(t)` 与 `t15/t10`，未增加无要求的扫描速率--初温全组合网格。
- [x] 已输出正文候选图、逐点绘图数据和 `T_0 | t15 | t10 | 相对37℃变化` 结果表。
- [x] 结果只解释衣料预热初温影响：`27 ℃` 相对 `37 ℃` 使 `t15/t10` 分别下降 `13.60%/11.98%`，说明实验前服装热状态是重要输入；未把 `T_0=37 ℃` 写成附件事实。

## 模型汇总规范修订（2026-08-27）
- [x] 按 `model_summary.md` 唯一标准重构 q1.tex 模型汇总：`\noindent\textbf{模型汇总}\par` 改为 `\subsubsection{模型汇总}`（三级编号标题固定例外）。
- [x] 模型汇总零文字化：删除自然语言解释段，改为单个 `\left\{...\right.` 大括号公式，集中列出控制方程、有效比热、初始条件、层间连续、双侧 Robin 边界及 `T_in/t15/t10` 输出定义，编号 `eq:q1-model-summary`。
- [x] 远端已拉取：`origin/feature/q1` 与本地一致（最近提交 `b1dd2fa`），无其他模块新成果需纳入。

## TODO
- [x] 完成首轮 Python 图组预览：DSC 双面板、扫描速率温度与阈值双面板；仅作为初版构图，未写入正文、未提交。
- [x] 按“先分析意义再画图”的新要求，完成基准工况的温度场—温度响应—固化进度共时间轴主图。仅使用冻结数据，输出 `传热与固化.png` 和独立绘图表，位置为 `results/EXPERIMENT/第一问图形设计_20260827/`；图中不将温度等值线称为固化前沿，不从没有无 PCM 对照的结果声称提升比例。已核对数据、阈值、接口连续性、字体和标注；用户已授权将图、绘图报告 `选图分析.md` 及两份复现脚本提交至远端，正文及正式结果未改动。
- [x] 已同步远端审查 `5377986`：旧二元共晶示意图不得作为正文候选；本次报告已说明，未擅自删除远端旧图，也未实施审查中的正文措辞修改。
- [x] 完成衣料初始温度灵敏度图与表，并将结果写入 Q1 灵敏度分析部分。
- [x] 本项为独立敏感性分析，Q1 主模型及 `T_0=37 ℃` 冻结基准结果不解冻；论文同时说明该基准不代表任意服装预热状态。
- [x] 正文继续明确：`T_in=T_1(0,t)` 是等效评价界面；`v0=5 K/min` 是显式建模假设；绝对潜热与 DSC 归一化形状来源分开表述。
- [x] 按 skill 模板重构 q1.tex 结构：`\subsection` 改为 `\noindent\textbf{大分点}\par` 格式，删除三级编号 `\subsubsection`。
- [x] 原第2节"几何、材料与人体边界参数"改名为"数据特征与预备处理"，新增 DSC 相变数据特征大分点。
- [x] 原第3节"人体--三层防护服耦合传热模型"改为大分点+小分点结构，新增"模型汇总"段。
- [x] 原第4节"微小时间段拓展分离变量求解"改为大分点结构，新增"模态方程统一求解与斜率闭合"大分点。
- [x] 原第5节"坚持时间与模型检验"改为"求解结果与分析"，下设"求解结果"、"模型检验"、"衣料初始温度灵敏度"三个大分点。

## DONE
- [x] 完成中间功能层相变示意图：采用传统二元相图结构，标出题面约 `25 ℃` 开始固化放热、约 `14.7 ℃` 固化完成；图中组分和相界仅作机理示意，不参与定量求解。
- [x] 完成服装初始温度灵敏度：`T_0=27/30/33/37 ℃` 时，`t15=236.644/248.807/260.075/273.908 s`，`t10=273.894/286.057/297.324/311.158 s`；运行门禁通过，结果位于 `results/EXPERIMENT/initial_temperature_sensitivity_20260827/`。
- [x] 完成有限相变语义修正、DSC 数据诊断、模态正演、有限体积独立验证和三组扫描速率敏感性。
- [x] 完成 `M=60` 独立核算；正式结果继续统一采用 `M=40`。
- [x] 按 skill 模板完成 q1.tex 全文结构重整：`\subsubsection` 全部改为大分点/小分点格式，新增 DSC 数据特征、模型汇总、模型检验等节段，保持全部数学公式、图表、引用不变。

## 预览修复（2026-08-27）
- [x] Q1 局部预览通过：`output/build/section-preview/q1/preview.pdf`（430 KB），来源 `modules/20_q1/paper/q1.tex`。
- [x] `q1_algorithm.tex` 由 `algorithmic`（algorithmicx 语法）转换为 `algorithm2e` 语法，算法步骤、数学量与结论完全保留；与 paper-shell 前导仅加载 `algorithm2e` 一致。当前工作区未提交。
- [x] 依赖修复：`feature/paper-shell` 前导 CJK 字体 `BoldFont=FandolSong-Bold.otf` 改为 `BoldFont=FandolSong-Bold`（提交 `a6c1eee` 已推送），解决 fontspec 混合“字体名+文件名”解析导致 `FandolSong-Bold.otf` 找不到的编译阻断；此问题同样影响全文预览，已被该修复一并解除。
