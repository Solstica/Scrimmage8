# Q4 审核结论（2026-08-27）

## 总体判断
Q4 已完成对 Q3 双阈值接口的适配，16 个场景的真实反求与边界复核均已完成；当前主要问题是 `q4.tex` 仍停留在旧 `15 ℃` 单阈值、三扫描速率正文，代码与论文尚未同步，因此不能冻结。

## P0 / 必须修正
- 正文不要再把阈值写死为 `15 ℃`。统一引入 `theta∈{15 ℃,10 ℃}`，定义 `t_{T,theta}^{(4)}(alpha)=inf{t:T_in^(alpha)(t)<=theta}`，实际时间为 `min(t_{T,theta}^{(4)},t_W^(4))`。
- Q3 目标必须写成同扫描速率、同阈值的 `t_{3,theta}^*(beta_DSC)`；Q4 优化约束应严格匹配同一 `(beta_DSC,theta)` 场景。
- `q4_algorithm.tex` 输入中补入阈值 `theta`，所有 `t_T^(4)` 改为 `t_{T,theta}^{(4)}`，避免算法仍默认 15℃。
- 旧正文结果 `43.787%/86.328%/157.288%` 仅是 `15 ℃` 阶段对照，不能继续作为唯一正式结果。若 Q3 最终锁定 `10 ℃`，则主三速率结果应更新为：`2 K/min: 52.594%`，`5 K/min: 108.984%`，`10 K/min: 203.375%`。
- 结果分析中“低速扫描下相变过程持续时间更长，因此潜热更大”表述不严谨。应改为：在相同 DSC 温度域峰面积下，由 `L_pcm=60A_DSC/beta_DSC`，较小的假设扫描速率对应更大的反演潜热，因此达到 Q3 目标所需强化倍率降低。

## P1 / 终稿统一
- 建议把放热能力倍率符号从 `alpha` 改为 `gamma`，避免与第二层热扩散率 `alpha_2` 冲突。若不改符号，至少在全文严格区分。
- 正文补充“不追加资金”的解释：Q4 不采用 Q3 的追加增厚预算，恢复原始 `0.7/0.4/0.3 mm` 厚度，只提高功能层本征放热能力；否则保留 Q3 最优增厚时 `alpha=1` 即可达标，问题会退化为 0% 提升。
- DSC 扫描速率符号统一为 `beta_DSC`，与 Q2 空气风速 `v_a` 区分。
- 低速高潜热场景只作数学诊断；5 个 `alpha=1` 零提升场景是 Q3 已受负重约束/原始厚度已达标的自然结果，不应误写成材料强化“无意义”。
- 图表不要全部塞进正文。优先保留双阈值需求图和最能说明相变倍率作用的一张材料响应图；达标边界/约束裕量更适合作为验证候选。

## 数值验证状态
- 已完成 8 扫描速率 × 2 阈值，共 16 场景真实反求。
- 5 个场景在 `alpha=1` 已达标；其余 11 个场景二分下界不可行、上界可行。
- 16 场景共 38 个原始倍率/边界工况独立复核通过，最大倍率区间宽度 `6.1035e-5`。
- 当前未对全部 16 场景逐一做 `M=60` 或半时间步复核；旧 `v=5,15℃` 的模态敏感性证据不能外推为所有场景已完成网格收敛。

## 分支职责提醒
- `feature/q4` 当前包含 Q3 正文/算法改动用于接口同步。最终合并时，Q3 正文应以 `feature/q3` 为唯一正式来源，避免 Q4 分支继续独立维护 Q3 文本。

## 审核状态
`模型：通过；双阈值接口：通过；16 场景数值：通过；正文同步：P0 未完成；冻结：否。`
