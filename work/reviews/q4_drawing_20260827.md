# Q4 绘图审核补充（2026-08-27）

## 结论
- 图组的数据变换、逐图 CSV、二分边界检查和 SVG/PNG 重绘检查通过。
- 当前绘图报告同时保留 Q4-1.1（三速率、15 ℃）与 Q4-1.2（8速率×双阈值）两套口径，终稿前需要重新分层；正文模型定义也必须先与双阈值结果统一。

## P0 调整
1. **重整绘图报告版本层级**：
   - A. 当前候选：Q4-1.2 双阈值接口与 16 场景结果；
   - B. 历史对照：Q4-1.1 三速率、15 ℃ 图组。
   不要让旧 1.1 在报告开头继续呈现为当前主版本。
2. **正文模型先参数化阈值再插入双阈值图**。当前 `q4.tex` 前部仍写死 15 ℃ 热安全时间，而结果表已包含 15 ℃/10 ℃ 两套口径。应统一定义：
   `t_{T,θ}^{(4)}(α)=inf{t:T_in^{(α)}(t)≤θ}`，并令 Q4 与 Q3 按同一 `(β_DSC, θ)` 场景继承目标。
3. **双阈值需求图只作当前诊断候选**。最终主阈值锁定后，正文版本应删除另一阈值行/面板；若保留双阈值版，只能放在阈值敏感性分析中。

## 选图与解释建议
1. **正文优先保留“约束裕量”图**：直接展示原始方案、达标方案和负重上限之间的关系，最能解释 Q4 的工程优化结果。
2. **“相变响应”作为第二主图候选**：必须继续强调它是 `c_eff(T,α)` 的模型本构切片，仅适用于相变推进分支；峰温不变来自同比例强化假设，不是独立实验发现。
3. **“达标边界”放验证/附录**：二分上下界只能解释数值最小性与收敛，不是统计置信区间，毫秒级端点裕量也不能解释为物理预测精度。
4. **“潜热需求”优先级较低**：`L_α=αL_pcm` 本身由模型定义直接给出，如正文已有结果表与相变响应图，可不再占正文主图位置。
5. **不要用三速率连线暗示连续扫描速率规律**。扫描速率是 DSC 条件假设，不是现场控制量；低速结构变化仍属于参数诊断。
6. **符号统一**：DSC 扫描速率统一写作 `β_DSC`；后续若整体正文改放热倍率符号，也需保证图、表、算法同步。

## 状态
`DRAWING REVIEW: DATA PASS / PAPER NEEDS REVISION`
