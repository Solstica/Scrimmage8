# Q2 绘图审核补充（2026-08-27）

## 结论
- 阈值时间图与数值验证图通过，均可作为正文候选。
- 外对流边界敏感性图的数据正确，但当前以旧 PMV 为零线，不适合终稿主模型叙事，需调整后再进正文。

## P0 调整
1. **敏感性图改用 ISO 简化关系作为参照基准**。
   当前图定义“旧 PMV=0”，而 Q2 主线拟采用 `h_e=18.46 W/(m²·K)` 的 ISO 11079 冷环境简化口径。终稿应改为：
   - ISO 简化 = 0；
   - 旧 PMV 与 Kuwabara 显示相对 ISO 主模型的阈值时间变化。
   这样图的逻辑与正式模型一致：更换外边界相关式会使主模型预测提前/延后多少。
2. **阈值时间图突出主模型层级**：ISO 简化作为主口径，应在视觉层级上突出；旧 PMV、Kuwabara 为对照，不应与主模型完全等权展示。
3. **符号统一**：
   - 空气风速使用 `v_a`；
   - DSC 扫描速率统一使用 `β_DSC`；
   图题、坐标轴、图注和数据说明不要再用同一个 `v` 表示两类速度。

## 其他建议
- 数值验证图继续明确：模态/FVM 差异与 FVM 加密变化是数值离散检查，不是实验误差，也不证明外对流公式物理适用性。
- 阈值时间图中的 15 ℃—10 ℃ 连线只表示同一工况的阈值配对，不得解释为可继续安全暴露时间。
- 若篇幅有限，优先保留“阈值时间 + 数值验证”；边界敏感性图在改为 ISO 基准后可放正文相关式选择段或附录。

## 状态
`DRAWING REVIEW: CONDITIONAL PASS`
