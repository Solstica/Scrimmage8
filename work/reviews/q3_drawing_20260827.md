# Q3 绘图审核补充（2026-08-27）

## 结论
- 三张诊断图的数据接口、构图逻辑和解释边界通过。
- 当前图组仍属于双阈值诊断，不应直接按“最终正文结果图”冻结；最终阈值确认后再裁剪。

## 调整意见
1. **主正文优先保留“热安全—负重权衡”图**。该图直接解释 Q3 的核心机制：
   `增加外层厚度提高热安全时间` 与 `增重降低允许负重时间` 的竞争。
   继续保留 `v=2,n=3` 下两类时间仅相差约 1.187 s 的标注，但不得写成精确交点。
2. **“有效时间全景”先作为团队诊断总览**。当前 8 个扫描速率 × 4 个层数 × 2 个阈值信息量大，适合审核完整离散枚举；最终阈值锁定后删除另一套阈值，并按正文实际采用的扫描速率域裁剪，再决定是否进正文。
3. **“双阈值切换区间”定位为参数敏感性/附录图**。横轴 DSC 扫描速率是缺失实验条件的假设参数，不是现场可操控的工程变量；图注必须明确这是扫描条件敏感性诊断，不能表述为通过调节扫描速率优化衣服厚度。
4. **切换区间保持区间语义**：只表示真实正演定位出的切换范围，不是统计置信区间；不得用平滑曲线、连续相区填色或取区间中点后冒充精确临界值。
5. **低速高潜热工况继续保持诊断边界**：可用于证明模型存在内部最优和约束切换，但不得直接当成可信材料设计方案；`260 kJ/kg` 等商业产品比较量不得画成通用物理上限。
6. **符号统一**：DSC 扫描速率统一写作 `β_DSC`。

## 最终选图建议
- 正文主图：热安全—负重权衡；
- 正文结果图：最终阈值确定后裁剪的有效时间/最优方案图；
- 双阈值切换区间：敏感性或附录候选。

## 状态
`DRAWING REVIEW: DIAGNOSTIC PASS`
