# Q1 审核结论（2026-08-27）

## 总体判断
Q1 主模型、数值验证和结果链已经完整，可继续保持 `FROZEN + CHECKED`。本轮不建议重开模型或重算主结果，只做终稿级措辞和图件清理。

## P0 / 必须修正
- 正文中“按附件‘放热为负’的约定”表述过强。附件数据确实表现为负峰，但未明确给出符号约定。建议改为：`附件 DSC 数据在相变区间表现为负峰，本文统一取其放热幅值为正，令 p_raw(T)=-1000D(T)`。
- 现有“传统二元相图/共晶相图”不应进入论文正文。附件只提供 DSC 温度—放热数据，不能支持虚构组分、液相线/固相线、共晶点、α/β 相区等信息。若需要相变图，只保留由数据直接得到的 `p(T)`、累计固化进度 `xi(T)`、权重 `w(T)` 等图。

## P1 / 终稿统一
- 全文 DSC 扫描速率符号建议从 `v` 统一改为 `beta_DSC` 或其他不与 Q2 空气风速冲突的符号。Q2 中空气风速建议使用 `v_a`。
- 继续明确：`T_in=T_1(0,t)` 是贴身侧等效评价温度，人体本体仍按附件固定为 `37 ℃`。
- `v0=5 K/min` 继续明确为显式建模假设，不得写成附件实验条件。
- 服装初始温度 `T0=37 ℃` 保留为基准假设；已有 `27/30/33/37 ℃` 灵敏度足以说明该假设的不确定性，不需要再扩展全组合扫描。

## 保持不变
- 三层一维瞬态导热 + 有限不可逆相变有效比热模型。
- 人体侧 `h_b=3 W/(m²·K)`、环境侧附件自然对流关系。
- 基准结果：DSC 扫描速率 `2/5/10 K/min` 下 `t15=520.903/273.908/191.642 s`，`t10=558.207/311.158/228.864 s`。
- 模态法、FVM、时间步、模态阶数与潜热上限验证结论均保持冻结。

## 审核状态
`模型：通过；数值：通过；正文：小修；图件：删除虚构二元相图后通过。`
