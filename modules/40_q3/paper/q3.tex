\section{问题三模型的建立与求解}

\subsection{问题的描述与分析}

问题三在问题一基础上增加最外隔热层厚度以延长实验者站立时间，约束为材料费用与负重能力。关键在于增厚同时加剧传热（延缓降温）与增重（缩短可站立时间）两类效应，且需分别考察$15^\circ\mathrm C$与$10^\circ\mathrm C$两种温度阈值下的最优设计。以每层固定$0.3\ \mathrm{mm}$的涂层数$n$作为离散决策变量，综合费用、负重与传热约束建立优化模型，对两类阈值分别求解最优层数，流程见图\ref{fig:q3-flow}。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\textwidth]{\ProjectRoot/modules/40_q3/figures/q3_flow.png}
\caption{问题三的求解流程}
\label{fig:q3-flow}
\end{figure}
\FloatBarrier

\subsection{PDE约束离散优化模型}

\noindent\textbf{1. 决策变量与预算可行域}\par

以新增最外层涂层数
\begin{equation}
n\in\mathbb Z_{\ge0},\qquad d_3(n)=0.3(1+n)\ \mathrm{mm}
\label{eq:q3-decision}
\end{equation}
作为离散决策变量。沿用问题一的有效传热面积$A=1.6521\ \mathrm{m^2}$，原防护服材料费用为
\begin{equation}
C_0=A\rho_1d_1p_1+10A+A\rho_3d_3(0)p_3\approx301.67\ \mathrm{yuan}.
\label{eq:q3-base-cost}
\end{equation}
其中$p_1=1000\ \mathrm{yuan/kg}$、$p_3=300\ \mathrm{yuan/kg}$。每增加一层$0.3\ \mathrm{mm}$最外涂层，增加质量$\Delta m\approx0.148689\ \mathrm{kg}$、费用$\Delta C\approx44.61\ \mathrm{yuan}$。由预算约束
\begin{equation}
C_0+n\Delta C\le1.5C_0
\label{eq:q3-budget}
\end{equation}
得到$n\le3$，故实际可行域为
\begin{equation}
\mathcal N=\{0,1,2,3\}.
\label{eq:q3-feasible-set}
\end{equation}

\noindent\textbf{2. 厚度决策对应的PDE状态约束}\par

给定$n\in\mathcal N$后，外层厚度$d_3(n)$改变第三层传热域，从而改变贴身侧温度响应。记方案$n$下三层温度场为$T_i^{(n)}(x,t)$，其状态方程为
\begin{equation}
\left\{
\begin{aligned}
&\rho_i c_i\frac{\partial T_i^{(n)}}{\partial t}
=k_i\frac{\partial^2T_i^{(n)}}{\partial x^2},\qquad i=1,3,\\
&\rho_2c_{2,\mathrm{eff}}(T_2^{(n)})\frac{\partial T_2^{(n)}}{\partial t}
=k_2\frac{\partial^2T_2^{(n)}}{\partial x^2},\\
&-k_1T_{1,x}^{(n)}(0,t)=h_b[T_b-T_1^{(n)}(0,t)],\\
&-k_3T_{3,x}^{(n)}(L_n,t)=2.38[T_3^{(n)}(L_n,t)+40]^{1.25},
\end{aligned}
\right.
\label{eq:q3-pde-state}
\end{equation}
其中$L_n=d_1+d_2+d_3(n)$，初始条件、两处界面的温度连续与热流连续条件以及功能层不可逆相变关系均沿用问题一。由该PDE正演得到贴身侧温度$T_1^{(n)}(0,t)$，并定义热安全时间
\begin{equation}
t_{\theta}(n)=\inf\{t>0:T_1^{(n)}(0,t)\le\theta\},
\qquad \theta\in\{15,10\}\,^{\circ}\mathrm C.
\label{eq:q3-thermal-time}
\end{equation}
因此，厚度$n$首先通过PDE状态方程改变$t_\theta(n)$，再进入优化目标。

\noindent\textbf{3. 负重约束与有效持续时间}\par

方案$n$下服装质量为
\begin{equation}
m_c(n)=A\left[\rho_1d_1+\rho_2d_2+\rho_3d_3(n)\right].
\label{eq:q3-clothing-mass}
\end{equation}
本文将题目中的“最大承受重量”理解为站立过程中实验者所支撑的总质量，因此将$m_h=60\ \mathrm{kg}$计入承重约束：
\begin{equation}
m_h+m_c(n)\le100-0.05t.
\label{eq:q3-weight-constraint}
\end{equation}
由此得到负重条件允许的最长站立时间
\begin{equation}
t_W(n)=20[40-m_c(n)].
\label{eq:q3-weight-time}
\end{equation}
热安全时间随隔热层增厚而增加，而负重允许时间随服装质量增加而缩短。故对每个阈值定义有效持续时间
\begin{equation}
t_{\rm eff,\theta}(n)=\min\{t_{\theta}(n),t_W(n)\}.
\label{eq:q3-effective-time}
\end{equation}

\noindent\textbf{4. 模型汇总}\par
综上，问题三构成一个以离散厚度为决策变量、以三层非稳态传热PDE为状态约束、以有效持续时间最大化为目标的PDE约束离散优化模型：
\vspace{-0.3\baselineskip}
{\boldmath
\begin{equation}
\max_{n}\quad t_{\rm eff,\theta}(n),\qquad \theta\in\{15,10\},
\label{eq:q3-summary-obj}
\end{equation}}
{\boldmath
\begin{equation}
\mathrm{s.t.}\quad
\left\{
\begin{aligned}
&n\in\{0,1,2,3\},\qquad d_3(n)=0.3(1+n)\ \mathrm{mm},\\
&C_0+n\Delta C\le1.5C_0,\\
&\rho_i c_iT_{i,t}^{(n)}=k_iT_{i,xx}^{(n)},\quad i=1,3,\\
&\rho_2c_{2,\mathrm{eff}}(T_2^{(n)})T_{2,t}^{(n)}=k_2T_{2,xx}^{(n)},\\
&-k_1T_{1,x}^{(n)}(0,t)=h_b[T_b-T_1^{(n)}(0,t)],\\
&-k_3T_{3,x}^{(n)}(L_n,t)=2.38[T_3^{(n)}(L_n,t)+40]^{1.25},\\
&t_\theta(n)=\inf\{t>0:T_1^{(n)}(0,t)\le\theta\},\\
&m_h+m_c(n)\le100-0.05t_{\rm eff,\theta}(n),\\
&t_W(n)=20[40-m_c(n)],\qquad t_{\rm eff,\theta}(n)=\min\{t_\theta(n),t_W(n)\}.
\end{aligned}
\right.
\label{eq:q3-summary-con}
\end{equation}}

\subsection{模型求解}

候选集合仅含四个离散方案，采用枚举法逐一正演。对每个$n$先按式\eqref{eq:q3-pde-state}求解温度场，得到$t_{15}(n),t_{10}(n)$，再计算$t_W(n)$与两种阈值下的$t_{\rm eff,\theta}(n)$，最后选取目标函数最大的层数。完整步骤见算法\ref{alg:q3-enumeration}。

\input{\ProjectRoot/modules/40_q3/paper/q3_algorithm.tex}

增加隔热厚度会提高$t_\theta(n)$，而服装增重使$t_W(n)$下降，因此最优方案由热安全收益、负重损失与预算可行域共同决定。

\subsection{求解结果与分析}

基准扫描速率$\beta_{\rm DSC}=5\ \mathrm{K/min}$下，两种评价阈值对应的最优方案均为$n^*=3$，即新增三层外涂层，最外层厚度为$1.2\ \mathrm{mm}$、总厚度为$2.3\ \mathrm{mm}$；$15\,^{\circ}\mathrm C$与$10\,^{\circ}\mathrm C$阈值下的最优有效持续时间分别为$416.04\ \mathrm s$和$490.64\ \mathrm s$。在$2$和$10\ \mathrm{K/min}$两组敏感性工况下，最优层数仍保持$n^*=3$，见表\ref{tab:q3-optimum}。

\begin{table}[!htbp]
\centering
\caption{基准及敏感性扫描速率下问题三最优方案}
\label{tab:q3-optimum}
\begingroup
\rowcolors{2}{gray!10}{white}
\begin{tabular}{ccccccccc}
\toprule
\rowcolor{cyan!50!blue!30}  % 青蓝色表头
$\beta_{\rm DSC}$ & \multicolumn{4}{c}{$15\,^{\circ}\mathrm C$阈值} & \multicolumn{4}{c}{$10\,^{\circ}\mathrm C$阈值}\\
\rowcolor{cyan!40!blue!60}  % 第二行表头同色
(K/min) & $n^*$ & $d_3^*$ (mm) & $t_{\rm eff}^*$ (s) & 限制 & $n^*$ & $d_3^*$ (mm) & $t_{\rm eff}^*$ (s) & 限制\\
\midrule
5 & 3 & 1.2 & 416.04 & 热安全 & 3 & 1.2 & 490.64 & 热安全\\
2 & 3 & 1.2 & 701.20 & 热安全 & 3 & 1.2 & 774.81 & 热安全\\
10 & 3 & 1.2 & 321.06 & 热安全 & 3 & 1.2 & 396.27 & 热安全\\
\bottomrule
\end{tabular}
\endgroup
\end{table}

基准及两组敏感性工况下，热安全约束均先于负重约束达到，最优解位于预算允许的厚度边界。其中$\beta_{\rm DSC}=2\ \mathrm{K/min}$、$n=3$、$10\,^{\circ}\mathrm C$阈值下$t_{10}=774.808\ \mathrm s$，与$t_W=775.994\ \mathrm s$仅差约$1.19\ \mathrm s$，已接近两类约束的交汇区域。

为比较全部离散厚度方案在不同扫描速率下的有效持续时间，图\ref{fig:q3-eff-panorama}以新增层数为横轴、扫描速率为纵轴，并用颜色表示有效持续时间。

\begin{figure}[!htbp]
\centering
\includegraphics[width=\FigureDoubleWidth]{\ProjectRoot/modules/40_q3/figures/图2：有效时间全景.png}
\caption{两种阈值下不同扫描速率与新增层数的有效持续时间全景}
\label{fig:q3-eff-panorama}
\end{figure}

由图\ref{fig:q3-eff-panorama}可见，在$2,5,10\ \mathrm{K/min}$三组工况下，新增层数增加均使有效持续时间提高，且$n=3$对应各行中的最大值；随扫描速率降低，整体有效持续时间同步增加，但最优层数保持不变。
\FloatBarrier

为解释$n=3$为何成为当前离散域内的最优方案，图\ref{fig:q3-tradeoff}进一步比较热安全时间与负重允许时间随新增层数的变化。

\begin{figure}[!htbp]
\centering
\includegraphics[width=\FigureDoubleWidth]{\ProjectRoot/modules/40_q3/figures/图1：热安全与负重权衡.png}
\caption{不同扫描速率下离散新增层数对应的热安全时间与负重时间}
\label{fig:q3-tradeoff}
\end{figure}

由图\ref{fig:q3-tradeoff}可见，隔热层增厚使热安全时间持续上升，而服装增重使负重允许时间缓慢下降；在$n=0\sim3$的预算可行域内，热安全约束仍先于负重约束达到，因此增厚带来的热防护收益占主导，最优方案落在预算允许的最大层数$n=3$。
\FloatBarrier

两种评价阈值在基准及两组敏感性工况下给出相同的最优层数，但对应有效持续时间不同，说明评价标准变化主要影响性能指标而未改变当前最优结构。

\subsection{灵敏度分析}

为考察更宽参数范围内的约束竞争，将扫描速率扩展至$\beta_{\rm DSC}=0.5,1.0,1.25,1.5,1.75\ \mathrm{K/min}$。随着扫描速率降低、反演潜热增大，热安全时间逐渐接近负重时间，最优新增层数开始发生切换。图\ref{fig:q3-layer-state}给出两种阈值下最优层数的分段变化。

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.44\textwidth]{\ProjectRoot/modules/40_q3/figures/图3：双阈值最优层数状态带状图.png}
\caption{两种阈值下最优新增层数随扫描速率的状态带与切换区间}
\label{fig:q3-layer-state}
\end{figure}

由图\ref{fig:q3-layer-state}可见，$15\,^{\circ}\mathrm C$阈值下$0\to1$、$1\to2$、$2\to3$的切换位置约为$1.22,1.36,1.54\ \mathrm{K/min}$，$10\,^{\circ}\mathrm C$阈值下约为$1.29,1.48,1.72\ \mathrm{K/min}$。这说明在高潜热区域，继续增厚带来的热安全收益逐渐受到负重损失制约，最优结构不再固定在预算上界。
\FloatBarrier

六处层数切换位置经半时间步长复核后方向均保持不变。切换区域对应的反演潜热约为$712$--$1004\ \mathrm{kJ/kg}$，表明在高潜热区间内隔热收益与增重损失的竞争会改变最优离散结构。