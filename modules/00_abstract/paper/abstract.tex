围绕题目给出的对象、数据与约束，建立从数据分析、数学建模到数值求解和结果检验的完整方法。开赛后将本段替换为全文任务概述，控制在约 2--3 行，只概括最终采用的模型主线和主要评价对象。

针对问题一，先完成必要的数据处理或机理分析，再建立\textbf{核心模型名称}，采用\textbf{核心算法名称}求解。结果得到\textbf{最关键结论或数字}，并用一句话说明该结果回答了题目的哪一项要求。正式写作时按“任务--模型--算法--结果--解释”压缩为约 4--6 行，不整句加粗。

全文特点最多保留两项真正贯穿各问的内容，例如统一的建模主线或大规模求解方法；不要把“数据驱动、层级结构、证据链”等内部概括当作亮点句。

\keywords{关键词1；关键词2；关键词3；关键词4}
