{
  "question": "Q4",
  "model_id": "Q4-M1",
  "model_version": "1.2",
  "status": "APPROVED",
  "objective_or_outputs": [
    "minimize alpha - 1 and report 100(alpha - 1)%"
  ],
  "variables": [
    "alpha >= 1: multiplier of the middle-layer heat-release capacity",
    "T_i(x,t): temperature field of layer i in degC"
  ],
  "parameters": [
    "t3_best(v,threshold) is read from the matching Q3 scan-rate and threshold row",
    "d = (0.7, 0.4, 0.3) mm from the original garment",
    "L_pcm(v) = 1223127.48/v J/kg; checked against Q3 candidate table for every input rate",
    "tW4 = 784.91566616 s derived from the Q3 weight formula"
  ],
  "equations_or_objective": [
    "c2_eff(T,alpha) = c2 + alpha*L_pcm*w(T) while solidification advances",
    "t_eff(alpha,v) = min(tT(alpha,v), tW4)",
    "for each (v,threshold), min alpha-1 subject to t_eff(alpha,v,threshold) >= t3_best(v,threshold)"
  ],
  "constraints": [
    "alpha >= 1",
    "t_eff(alpha,v,threshold) >= t3_best(v,threshold)",
    "middle-layer solidification progress is nondecreasing"
  ],
  "initial_or_boundary_conditions": [
    "all layers start at 37 degC",
    "body boundary: T_b=37 degC and h_b=3 W/(m2 K)",
    "outer boundary: T_inf=-40 degC with Q1 natural convection",
    "interface temperature and heat flux are continuous"
  ],
  "data_scope": [
    "attachment 1 DSC temperature and heat-flow columns",
    "Q3 764375a coarse optimum rows: v=0.5,1,1.25,1.5,1.75,2,5,10 K/min",
    "Q3 candidate table supplies L_pcm at the same scan rate and selected layer count",
    "15 and 10 degC thresholds are separate conditional diagnostics; no final threshold is selected"
  ],
  "required_outputs": [
    "minimum alpha at every Q3 scan-rate/threshold pair, including alpha=1",
    "minimum increase percentage at every scan rate",
    "thermal and effective endurance times",
    "binary lower and upper bounds supporting the discretized search result, not a global proof"
  ],
  "allowed_numerical_freedom": [
    "time step and modal truncation",
    "quadrature order",
    "binary-search tolerance"
  ],
  "forbidden_silent_changes": [
    "change the Q1 finite latent-heat model back to a persistent source",
    "change the original three layer thicknesses",
    "mix Q1 latent heat and Q3 target times from different scan rates",
    "mix Q3 and Q4 threshold definitions",
    "cap alpha without a proved bound",
    "replace the hard endurance constraint by a penalty"
  ],
  "modeler_notes": "Interface update requested by user on 2026-08-27. Q4 v1.2 inherits the already approved Q3 dual-threshold diagnostics without selecting a final objective. Source: origin/feature/q3 764375a, work/tasks/q3.md and the approved low-rate model interface. Old v1.1 results are retained as 15 degC stage references. Low-rate scenarios are not certified material designs."
}
