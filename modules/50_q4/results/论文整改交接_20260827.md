# Q4 论文整改交接（2026-08-27）

本文件只供 Q4 论文手人工整改，不替建模者决定阈值、扫描速率范围或正式结果。对照当前 `q4.tex`、2025 A 易良禹论文呈现方式及论文兽 v1.4.0 规则整理。

## P0：把准备性推导迁入 X.2

当前 X.3 “最小放热能力提高比例模型”前部仍包含较长的 DSC 同比例增强与给定 `α` 的传热正演。建议整理为：

```text
4.1 问题的描述与分析
4.2 预备工作
    1. DSC 放热能力同比例增强
    2. 给定 α 的传热正演
    3. 坚持时间函数定义
4.3 最小放热能力提高比例模型
    1. 决策变量
    2. 目标函数
    3. 约束条件
    4. 模型汇总
4.4 模型求解
4.5 求解结果与分析
4.6 灵敏度分析
```

- [ ] 将 `p_α(T)`、`L_pcm^(α)`、`c_eff^(α)` 的推导与给定 `α` 的正演关系迁入 `4.2 预备工作`；X.3 只保留最终优化变量、目标和约束。
- [ ] 不删除这些推导，只调整位置并压缩重复说明。

## P0：模型命名句与模型汇总

- [ ] 在模型汇总前补一句最精炼模型命名句，建议人工改成类似“综上，我们建立最小放热能力提高比例模型。”
- [ ] 模型汇总标题后继续严格零文字，保持“目标函数公式 + `s.t.` 左大括号约束公式”。
- [ ] 人工审核模型命名句：与决策变量 `α`、最小提高比例目标和坚持时间约束一致，不使用“完整、统一、综合、闭环、体系、框架、协同”等包装词。

## P0：伪代码语言回调

若当前 `q4_algorithm.tex` 为全英文主体：

- [ ] 保留 `Input / Output / Initialize / Return`、`for / while / if / else` 为英文；
- [ ] 算法标题改中文；
- [ ] Input / Output 后的说明改中文；
- [ ] `Compute / Evaluate / Update / Select / Return infeasible` 等动作改成中文数学叙述；
- [ ] 数学符号和二分搜索逻辑不变。

## P1：清理 GPT 留档式否定句

- [ ] X.1 “但不再追加材料资金，而是通过改进……”改为正向句，例如“在材料资金固定为原方案的条件下，通过提高中间功能层单位质量放热能力达到目标坚持时间。”
- [ ] “本问只改变……不改变……”若只是对旧方案的回应，改成“服装厚度、面积和质量保持原设计，仅以 `α` 调整相变潜热尺度。”
- [ ] 模型求解中“无需使用高维启发式方法”改为“由于设计变量为单变量且坚持时间关于 `α` 单调，采用二分搜索求解。”
- [ ] 其他用于解释弃用方法的否定句删除；真实可行性约束保留。

## P1：公式碎片化

- [ ] X.1 中 `r=α-1` 等短定义优先行内。
- [ ] 二分搜索中的中点公式、每段 `c_eff` / `α_2` 更新若只是求解实现且不需独立引用，可合并到算法或同一公式组，避免连续短 equation。
- [ ] 预备工作中的相变比例关系可按“权重定义 → 潜热倍率 → 有效比热”保留关键式，不给所有简单代入单独编号。

## P1：结果可见性与符号

- [ ] `4.5` 第一段第一句直接报告当前正式采用条件下的最小提高比例；阶段诊断场景不要让评委从 16 行表中自行寻找主答案。
- [ ] DSC 扫描速率统一写为 `\beta_{\rm DSC}`，空气速度保留 `v_a`。
- [ ] 当前 figures 已较多；正文只选一张最能说明提高倍率需求的主结果图，另可补一张极简“Q3 目标时间 → α → Q1 正演 → 可行性 → 二分”的流程图，避免继续堆结果图。

## 建模侧另行确认（论文手不要自行修改）

- [ ] 当前正文热安全时间公式若仍只定义 `15℃`，而结果继续报告 `15℃ / 10℃` 两组场景，需要建模手统一最终数学定义与正式结果使用范围。
- [ ] Q3 最终阈值、可信扫描速率范围和正式最优方案锁定后，再确认 Q4 是否需要重跑；论文手不得自行挑选。

## 完成后

- [ ] 重新执行 v1.4.0 `output_language_guard.md`。
- [ ] 重新执行 `output_structure_guard.md`，重点处理 `X2_PREWORK / MODEL_NAMING_SENTENCE / FORMULA_FRAGMENTATION / RESULT_DENSITY`。
- [ ] preview 后人工审核模型命名句、模型 / 结果一致性、公式密度和 Algorithm 语言。