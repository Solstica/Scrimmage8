# Q2 论文整改交接（2026-08-27）

本文件只供 Q2 论文手人工整改，不改模型、不重算结果。对照当前 `q2.tex`、2025 A 易良禹论文呈现方式及论文兽 v1.4.0 规则整理。

## P0：结构重排

当前正文仍把“外表面对流修正、人体侧修正、完整传热模型”拆成多个平级二级标题。建议整理为：

```text
2.1 问题的描述与分析
2.2 预备工作
    1. 外表面对流系数确定
    2. 人体侧换热参数确定
2.3 有风轻微运动条件下三层防护服瞬态传热模型
    1. 控制方程
    2. 人体侧边界
    3. 环境侧边界
    4. 初始与层间条件
    5. 模型汇总
2.4 模型求解
2.5 求解结果与分析
2.6 灵敏度分析
2.7 数值收敛与复算检验（确有独立内容时）
```

- [ ] 将 ISO 11079、PMV / Kuwabara 对照、人体系数确定等准备性内容集中到 `2.2 预备工作`。
- [ ] 将当前 `\subsubsection{模型汇总}` 改为 X.3 内最后一个编号大分点。
- [ ] 在模型汇总之前补一句最精炼模型命名句，例如“综上，我们建立有风轻微运动条件下三层防护服瞬态传热模型。”；标题后继续零文字。
- [ ] 人工审核模型命名句：模型名准确、简洁、与最终 PDE 与边界条件一致，无包装词。

## P0：伪代码语言

当前 `q2_algorithm.tex` 主体数学动作基本已经是中文，方向正确；只需统一固定关键字：

- [ ] `输入` → `Input`；
- [ ] `初始化` → `Initialize`；
- [ ] `输出` → `Output`；
- [ ] `return` 统一为 `Return`；
- [ ] `for / while / if / else` 保持英文；
- [ ] 循环体中的“计算、更新、组装、推进、插值”等继续使用中文，不翻成英文。

## P1：清理解释性否定与研发痕迹

- [ ] X.1 “不再引入动态人体热容节点，也不再使用……”改为直接写当前人体恒温边界与有限相变模型。
- [ ] “不另外引入题目未给定的代谢产热参数”若只是解释未采用方案则删除，直接写轻微运动通过附件给定的 `h_b=4.0` 表征。
- [ ] “问题二不再建立 `T_h(t)`”删除，直接写当前人体边界。
- [ ] “旧PMV”改为“PMV 对流关系”或“PMV 对照关系”，不带版本历史色彩。
- [ ] 真实物理约束或题面禁止条件可保留。

## P1：公式碎片化

- [ ] `h_b=4.0`、`T_b=37℃` 等单个短赋值优先行内，不独立占编号公式。
- [ ] 外对流系数推导中若多个短公式只用于得到最终 `h_e=18.46`，可在 `2.2 预备工作` 中合并展示，保留真正需要后文引用的关键式。
- [ ] 模型主体只保留控制方程、有效比热、初始 / 界面 / 双侧边界等关键公式。

## P1：结果可见性

- [ ] `2.5` 第一段第一句直接报告主采用外对流关系下的返回时间，不先让评委看表。
- [ ] 外对流关系比较进入 `2.6 灵敏度分析`；有限体积复算进入 `2.7 数值收敛与复算检验`，不要混在一个“模型检验”标题下。

## 完成后

- [ ] 重新执行 v1.4.0 `output_language_guard.md`。
- [ ] 重新执行 `output_structure_guard.md`，重点处理 `STRUCT.X2_PREWORK / MODEL_SUMMARY_LEVEL / FORMULA_FRAGMENTATION / MODEL_NAMING_SENTENCE`。
- [ ] preview 后人工检查模型命名句、公式密度和 Algorithm 语言。