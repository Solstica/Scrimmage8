# Q2 结果与图表交接

## 最新交接入口

2026-08-28 主结果数据已补齐：新增 [降温过程.csv](../figures/editable/降温过程.csv)（464行、4列）和 [两问对比.csv](../figures/editable/两问对比.csv)（3行、7列），各对应一张独立图，不再把两部分混成上一轮组合总表。降温表含ISO、5 K/min的贴身侧温度历程与两个阈值事件；对比表含两问在2/5/10 K/min下的t₁₅、t₁₀和相对Q1的缩短幅度。基准工况缩短幅度分别为62.2167%、62.8273%。布局和列角色见唯一 [绘图报告](../figures/绘图报告.md)。

本轮保真、数值运行和表格运行门禁均PASS；新历程的两个首次交点与已有ISO阈值一致，未平滑或重采样。已核对两问材料、初温、DSC处理、潜热和阈值定义，边界保持各自题设，不将差异归因于风速单因素。Q2原始DSC附件缺失，本次只读使用Q1工作树中的原件，来源记录在 `主图运行报告.json`；未修改Q1或跨责任域补原件。

新增登记只覆盖历程数值复现和跨问数据比较，不改变Q2主模型整体的DRAFT + NEEDS_REVIEW。当前完成的是CSV与说明，未渲染、未操作Origin、未修改正文或手工工程，未提交推送。

2026-08-28 命名统一：参照 Q1 实际 CSV 和用户成图，共用表头为“扫描速率（K/min）”“t₁₅（s）”“t₁₀（s）”，间隔列为“t₁₀−t₁₅（s）”。百分比列保留相对变化、相对偏差及加密变化幅度的含义。绘图说明中的轴名、图例同步；三份 CSV 除首行外逐字不变，图片与工程未改动。下文 β_DSC 是正文符号，不再叠加到表头。

当前 [绘图报告](../figures/绘图报告.md) 与三份 Origin CSV 已按建模手意见更新，数据仍统一放在 `figures/editable/`。阈值时间表只保留 ISO 主关系的四列；数值验证表只保留 ISO 方法复核与加密检查；敏感性表仅列 PMV、Kuwabara 相对 ISO 主关系的变化。表头采用 β_DSC、t₁₅、t₁₀ 及单位，不带“旧”“简化”或问题编号。此前的 SVG/PNG 与手工 OPJU 均未改动，新 CSV 不代表已生成新图。

数据来自 `91d96f4+Q1-3a1982f` 的已冻结相关式核算与有限体积验证，不使用下面旧 PMV 单一结果冒充主模型答案。正文与算法已采用 `18.46 W/(m²·K)`，但主模型单独登记仍为 `DRAFT + NEEDS_REVIEW`；本次排表不改变该状态。当前正文的敏感性图注仍写 PMV 分母，应后续按建模手意见同步为 ISO，不能用旧文案反向更改新表。数据核对不等于整问冻结，也不验证经验式的物理适用性。

以下保留旧 PMV 对照的参数和结果说明，便于核对差异。

## 模型与方法

- 结果 ID：`Q2-d50852b-Q1-3a1982f-20260827`
- 模型 ID / 版本：`WHUT13-Q2-THERMAL` / `d50852b+Q1-3a1982f`
- 模型公式来源：Q2 建模版本 `d50852b`，并继承 Q1 冻结版本 `Q1-3a1982f-20260826`
- 实际求解方法：微小时间段模态推进；每步通过 `2x2` 界面斜率方程保持温度与热流连续
- 对照参数：`dt=0.25 s`、`M=40`、Gauss 积分点 `160`、`h_b=4.0 W/(m2 K)`、`h_e=12.1 sqrt(3)=20.9578 W/(m2 K)`
- 阈值定义：贴身侧服装温度 `T_in=T_1(0,t)` 首次不高于 `15 degC` 或 `10 degC` 的时间

## 旧关系对照结果

| DSC 扫描速率（K/min） | 潜热（kJ/kg） | `t15`（s） | `t10`（s） |
|---:|---:|---:|---:|
| 2 | 611.564 | 171.403 | 182.234 |
| 5 | 244.625 | 92.670 | 103.439 |
| 10 | 122.313 | 66.481 | 77.207 |

旧 PMV 关系的对照数字从 `计算结果.csv` 读取。`温度历程.csv` 提供三种扫描速率的温度历程，`温度剖面.csv` 提供 5 K/min 情形的三层温度剖面。该组数字不得作为最终 Q2 结论。

## 已完成验证

- 模态时间步减半和模态数增加检查；
- 三种外对流关系、三种扫描速率共 9 个工况的独立隐式有限体积对照，阈值最大偏差 `1.2146%`；
- 有限体积步长和网格加密检查；
- 完整 `max(h_nat,h_for)` 与固定强制支路对照；
- 固化进度单调、潜热上界、界面热流残差和输出有限性检查。

详细数字见 `验证报告.md`、`外对流有限体积验证.csv` 和相应旧关系 CSV。外对流有限体积验证由外部 `run_gate.py` 审计，报告为 `外对流有限体积运行报告.json`，状态 `PASS`。

## 论文使用边界

推荐正文表：三种 DSC 扫描速率的潜热、`t15`、`t10` 对照表。

当前图表定位以上方最新交付为准：正文先展示 ISO 降温过程和两问阈值对比，验证只作支持。PMV/Kuwabara 相对 ISO 的敏感性不是三个并列主模型。旧温度曲线与温度剖面只覆盖 PMV 对照，不能作为主结果图；新过程数据来自 `主模型历程.json`。跨问比较条件已核对，保留两问各自题设边界，不得把工况总差异解释为风速单因素效应。

用于 Origin 绘图的文件为 [阈值时间.csv](../figures/editable/阈值时间.csv)、[边界敏感性.csv](../figures/editable/边界敏感性.csv)、[数值验证.csv](../figures/editable/数值验证.csv)，一图一表，首行列名、后面纯数据。阈值表为3×4，敏感性表为3×5；验证表为3×6，其中 A:C 与 D:F 是各自独立的子图数据，D:F 两行均对应 ISO、5 K/min，第三行留空，不与 A 列按行配对。列对应见绘图报告。本次未渲染、未启动 Origin，未修改原始核算和验证结果。

允许表述：旧 PMV 外对流关系下的上述阈值时间已通过数值收敛与独立有限体积复核，可作为外对流相关式敏感性的对照基线。

禁止写入：把 `h_e=20.9578 W/(m2 K)` 或 `t15=92.67 s` 写成最终唯一结论；动态人体热容节点；以固定 `37 degC` 人体源温度作为阈值；无限 `q_pcm(T)` 功率源；实地实验验证或超出当前参数范围的外推结论。

三关系核算见 `外对流核算.csv`，模态运行审计见 `外对流运行报告.json`，有限体积验证及审计见 `外对流有限体积验证.csv`、`外对流有限体积运行报告.json`。该敏感性对照及数值验证已冻结；最终主模型交接仍须落实最新审核后更新。绘图运行审计为 `绘图数据运行.json` 和 `绘图运行报告.json`，均为 `PASS`；绘图包未自动插入正文。
