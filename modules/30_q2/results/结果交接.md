# Q2 正式结果交接说明

## 模型与方法

- 结果 ID：`Q2-d50852b-Q1-3a1982f-20260827`
- 模型 ID / 版本：`WHUT13-Q2-THERMAL` / `d50852b+Q1-3a1982f`
- 模型公式来源：Q2 建模版本 `d50852b`，并继承 Q1 冻结版本 `Q1-3a1982f-20260826`
- 实际求解方法：微小时间段模态推进；每步通过 `2x2` 界面斜率方程保持温度与热流连续
- 正式参数：`dt=0.25 s`、`M=40`、Gauss 积分点 `160`、`h_b=4.0 W/(m2 K)`、`h_e=12.1 sqrt(3)=20.9578 W/(m2 K)`
- 阈值定义：贴身侧服装温度 `T_in=T_1(0,t)` 首次不高于 `15 degC` 或 `10 degC` 的时间

## 正式结果

| DSC 扫描速率（K/min） | 潜热（kJ/kg） | `t15`（s） | `t10`（s） |
|---:|---:|---:|---:|
| 2 | 611.564 | 171.403 | 182.234 |
| 5 | 244.625 | 92.670 | 103.439 |
| 10 | 122.313 | 66.481 | 77.207 |

正式数字只从 `计算结果.csv` 读取。`温度历程.csv` 提供三种扫描速率的温度历程，`温度剖面.csv` 提供 5 K/min 情形的三层温度剖面。

## 已完成验证

- 模态时间步减半和模态数增加检查；
- 三种扫描速率的独立隐式有限体积对照；
- 有限体积步长和网格加密检查；
- 完整 `max(h_nat,h_for)` 与固定强制支路对照；
- 固化进度单调、潜热上界、界面热流残差和输出有限性检查。

详细数字见 `验证报告.md` 和相应 CSV。正式运行由外部 `run_gate.py` 审计，报告为 `运行报告.json`，状态 `PASS`。

## 论文使用边界

推荐正文表：三种 DSC 扫描速率的潜热、`t15`、`t10` 对照表。

推荐正文图：三种扫描速率的贴身侧温度曲线；5 K/min 情形的三层温度剖面；Q1/Q2 在同一扫描速率下的贴身侧温度或外侧热流对比。

允许表述：在已确认的一维传热模型和经验对流关系下，报告上述阈值时间，并说明数值离散已通过收敛与独立有限体积复核。

禁止写入：动态人体热容节点、以固定 `37 degC` 人体源温度作为阈值、无限 `q_pcm(T)` 功率源、Q1 自然对流边界、未经批准的替代风速公式、实地实验验证或超出当前参数范围的外推结论。

`modules/30_q2/paper/q2.tex` 当前仍含旧模型表述，完成同步前不得直接引用其中公式或旧数字。
