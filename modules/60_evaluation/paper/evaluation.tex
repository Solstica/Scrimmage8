\section{模型总结与评价}

\subsection{模型总结}

用一段话串联各问模型之间的关系，说明问题一提供了什么基础、后续问题如何扩展，以及最终输出了哪些核心结果。这里强调“模型链条”，不要把摘要中的各问内容重新逐段复制。

\subsection{模型优点}

\begin{enumerate}[label=\textbf{\arabic*.}]
\item \textbf{结构清晰。} 各问题围绕统一对象逐步增加信息、约束或机制，避免方法之间相互割裂。
\item \textbf{结果可追溯。} 正文中的关键数值由当前代码和结果登记生成，并通过图表或检验量进行交叉说明。
\end{enumerate}

\subsection{模型局限}

\begin{enumerate}[label=\textbf{\arabic*.}]
\item \textbf{数据或先验仍有限。} 当前结论受题目所给样本范围和假设条件限制，超出适用范围时需重新校准。
\item \textbf{部分机制采用简化表达。} 为控制模型复杂度，对次要因素作了近似处理；若获得更多数据，可进一步细化。
\end{enumerate}

\subsection{改进方向}

后续可根据题型补充更多观测、独立验证或更精细的机制项，并在保持参数可辨识性和模型可解释性的前提下扩展模型。模型评价宜具体对应本文的实际处理，不写与全文无关的通用套话。
